  - **23/07/2026 (Brasília) — Layout do OrderCard OFR (linha de botões abaixo):** Em `src/pages/FreightOrders.tsx` o `OrderCard` foi reorganizado: o container externo virou `flex-col` em todos os breakpoints e a coluna lateral de botões (que em xl ocupava ~40% da largura e espremia coleta/entrega/itens) foi substituída por uma linha única de botões abaixo do bloco de informações, com `flex-wrap` para quebrar naturalmente. Botões padronizados em `h-10`, "Cancelar" alinhado à direita via `ml-auto`. Info agora usa 100% da largura do card em desktop, mostrando artigos/rateio/coleta/entrega sem truncar. Nenhuma mudança de comportamento — apenas layout. tsgo limpo.
