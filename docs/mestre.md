- **08/08/2026 (Brasília) — Mecânica: Remoção de Skeletons e otimização visual:**
    - Removidos os `OrderCardSkeleton` de todas as abas de Mecânica (OM, OC, OE e OT) por solicitação do usuário, visando reduzir a percepção de demora no carregamento.
    - Implementado um spinner discreto com animação de pulso e texto informativo durante o carregamento dos dados, garantindo feedback imediato sem o "salto" visual dos skeletons.
    - Mantido o isolamento definitivo de abas implementado anteriormente.
    - Ajustado o estado de carregamento para evitar a exibição de dados parciais ou incorretos durante a transição entre abas.
- **08/08/2026 (Brasília) — Ordem de Frete (OFR): Registro Manual de fretes realizados:**
1:     - Implementado o botão "Registro Manual" que abre um modal idêntico ao de "Nova OFR", mas com o campo obrigatório "Data do Frete Realizado".
2:     - OFRs criadas via Registro Manual são salvas diretamente com status `completed` e utilizam a data informada como `created_at` e `completed_at`, garantindo que apareçam no mês correto dos relatórios de faturamento.
3:     - Atualizada a mutation `createOrder` no `useFreightOrders` para suportar `manual_date` e `status` customizado.
4: - **06/08/2026 (Brasília) — Correção de contagem e isolamento OM/OC/OE e erro de coluna em OT:**
    - **Mecânica (OM/OC/OE):** As Ordens Elétricas e de Corretiva agora são filtradas corretamente por aba, incluindo os badges de contagem. A RPC `get_mecanica_stats` foi atualizada para retornar contagens separadas por modo (`om`, `oc`, `oe`), evitando a soma de todos os tipos em todas as abas.
    - **Mecânica (OM/OC/OE):** A RPC `get_maintenance_orders_list` recebeu o parâmetro `p_mode` para isolar os tipos de ordem em suas abas respectivas (OM: preventiva/artigo/agulha; OC: corretiva; OE: elétrica).
    - **Mecânica (OT):** Corrigido erro `column "notes" does not exist` na listagem de Troca de Artigo (OT). A RPC `get_article_change_orders_list` foi atualizada para usar a coluna correta `observations`.
- **06/08/2026 (Brasília) — Ordem de Faturamento (OF): anexo de foto obrigatório na coleta:** No modal de finalização da coleta (`OfCollectPhotosModal.tsx`), o anexo de pelo menos 1 foto das notas fiscais/documentos passou a ser obrigatório. O botão "Confirmar Coleta" agora valida a presença de fotos e exibe um alerta caso o usuário tente finalizar sem anexos. O texto de instrução no modal foi destacado com um aviso visual (cor âmbar) reforçando a obrigatoriedade. Verificado: tentativa de coleta sem foto bloqueada com toast de erro amigável; coleta com foto segue funcionando normalmente. `tsgo --noEmit` limpo.
 - **02/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) — saídas de OF não espelhadas e saída manual sem validação:** Nova auditoria do módulo (2.283 movimentos; zero `pieces`/`weight_kg` negativos, zero `on_machine` sem máquina, zero nulos de `company_id`/`client_id`/`article_id`, zero espelho órfão, zero `billing_order_id` inválido; todas as RPCs com `SECURITY DEFINER`, `search_path` fixo, guarda de tenant e EXECUTE só para `authenticated`/`service_role`; funções de trigger só para `service_role`). **Bugs encontrados e corrigidos:** (1) **446 saídas de OF nunca espelhadas no estoque manual** — o gatilho `mirror_of_to_manual_stock` só passou a copiar movimentos do tipo `out` em 27/07/2026 19:30, então 353 OFs coletadas entre 17/06 e 27/07 tinham `reserve`/`release` espelhados mas **nenhuma saída**, deixando o razão manual incompleto e o KPI "Entregue (OF coletadas)" subestimado; feito backfill (`INSERT ... SELECT` de `stock_movements` com `source_movement_id`, preservando `created_at`/`created_by`, ignorando segunda qualidade e com `ON CONFLICT DO NOTHING`). Simulação antes/depois confirmou **zero alteração de saldo** (todas essas saídas são anteriores ao primeiro lançamento manual, 27/07 19:31, e são absorvidas pela trava cronológica em zero) — mudou apenas o histórico de Movimentações e o total entregue dos meses anteriores (34.063 pç / 707.372,20 kg). (2) **saída manual da expedição sem validação de saldo** — `save_manual_stock_manual_entry` só validava o palete "em máquina"; um `adjust_out` de expedição podia exceder o disponível e criar histórico negativo, mascarado na tela pelo clamp em zero. Migration: a RPC passou a calcular o saldo físico da expedição com a **mesma linha do tempo/`eff_at`/trava em zero** de `get_manual_stock_estoque`, descontar o palete em máquina e recusar o lançamento com `insufficient_stock`; frontend (`src/pages/StockMalhaManual.tsx`) traduz o erro para "Quantidade maior que o saldo disponível na expedição desse artigo/máquina". REVOKE `PUBLIC`/`anon` e GRANT `authenticated`/`service_role` reaplicados. Verificado no preview: página carrega, KPIs coerentes e saldos por cliente/artigo idênticos aos de antes do backfill. `tsgo --noEmit` limpo.
 - **02/08/2026 (Brasília) — Estoque Malha (Manual): lançamento manual deixa de ser engolido por reservas/saídas de OF antigas:** Usuário reportou que a entrada manual de 02/08/2026 09:55 (Bil têxtil · RIBANA 2x1 EXCLUSIVE · TEAR 33 · 46 pç) não aparecia no disponível. Diagnóstico: o artigo tinha histórico manual negativo (saídas espelhadas das OFs coletadas #341/#433 e reservas pendentes das OFs #339/#340, total de 115 pç, feitas quando o módulo manual ainda não tinha nenhuma entrada) — o "Disp." era `estoque − reserva total`, então as 92 pç lançadas (46+46) eram integralmente consumidas por reservas de paletes que nunca foram registrados no estoque manual, exibindo 0. **Correções (migration em `get_manual_stock_estoque`):** (1) o disponível deixou de ser calculado por subtração direta da reserva e passou a usar uma **linha do tempo por cliente+artigo+máquina** com trava cronológica em zero (`GREATEST(0, Σdeltas − LEAST(0, menor prefixo))`), incluindo entradas/saídas de expedição e as reservas pendentes — reserva feita em período sem estoque manual é absorvida na época e não consome lançamentos posteriores, enquanto reserva feita sobre estoque existente continua baixando o disponível na quantidade exata; o disponível segue limitado ao estoque físico da expedição e somado ao palete "em máquina" (nunca consumido por reserva); removidos o `art_excess`/`alloc` de rateio anterior. (2) As **saídas geradas pela coleta de OF** passaram a ser posicionadas na linha do tempo pela data da **primeira reserva** daquela OF/artigo/máquina (`eff_at`), e não pela data da coleta — assim uma OF separada de paletes não registrados no manual não apaga entradas feitas depois; a mesma ordenação vale para o cálculo do estoque (`phys`). Verificado no preview: 83 combinações cliente+artigo, zero divergência entre a soma das máquinas e o total do artigo (peças e kg), zero valor negativo, e a entrada do usuário passou a aparecer (RIBANA 2x1 EXCLUSIVE: Est. 92 pç · Disp. 92 pç). Observação: as entradas manuais desse artigo foram lançadas só em peças (peso 0), por isso os KPIs em kg seguem zerados até que o peso seja informado. REVOKE `PUBLIC`/`anon` e GRANT `authenticated`/`service_role` reaplicados.
 - **02/08/2026 (Brasília) — Pente fino Estoque Malha (Manual):** Auditoria completa do módulo após as últimas entregas. Banco íntegro: 2.283 movimentos, zero `pieces`/`weight_kg` negativos, zero `on_machine` sem máquina, zero `company_id`/`client_id`/`article_id` nulos, zero espelho órfão de `stock_movements`, zero movimento com artigo de outra empresa; RPCs de leitura e escrita com `SECURITY DEFINER`, `search_path` fixo e guarda de tenant. **Bugs encontrados e corrigidos (migration):** (1) **paginação instável em Movimentações** — `get_manual_stock_movements` ordenava só por `created_at DESC` e há 132 timestamps repetidos na base (lançamentos gravados no mesmo instante pelas RPCs de OF), o que fazia registros se repetirem ou sumirem ao trocar de página; adicionado desempate por `id DESC` na subconsulta e no `jsonb_agg`; (2) **filtro por cliente divergente entre abas** — a aba Movimentações filtrava por `m.client_id` puro enquanto o Estoque usa `COALESCE(m.client_id, articles.client_id)`; passou a usar o mesmo fallback (filtro, contagem e nome exibido); (3) **espelho de OF sem cliente** — `mirror_of_to_manual_stock`/`mirror_of_update_to_manual_stock` copiavam `client_id` de `stock_movements` (coluna anulável); agora, se vier nulo, preenchem com o cliente do artigo, evitando registro invisível nos filtros; o trigger de INSERT ganhou `ON CONFLICT (source_movement_id) DO NOTHING` (idempotência em reprocessos); (4) **perda de peso/peças na transferência do palete** — em "Recontar e lançar para a expedição", informar só peças (ou só peso) zerava a outra unidade do palete e ela desaparecia do estoque; agora, no modo transferência, a unidade não informada preserva o valor atual do palete; (5) **funções internas de trigger** `mirror_of_to_manual_stock` e `mirror_of_delete_to_manual_stock` eram executáveis por `authenticated` (a de UPDATE já não era) — EXECUTE revogado de `PUBLIC`/`anon`/`authenticated` e mantido só para `service_role`. Verificado no preview: página Estoque Malha (Manual) carrega normalmente (KPIs e agrupamento por cliente/artigo). `tsgo --noEmit` limpo.
 - **02/08/2026 (Brasília) — Estoque Malha (Manual): soma das máquinas volta a bater com o "Disp. Rolos" do artigo:** Usuário reportou (Bil têxtil · MALHA EXCLUSIVE) soma de 1.147 peças nas máquinas contra 1.043 exibidos no artigo. Causa: em `get_manual_stock_estoque` o disponível **por máquina** descontava só a reserva da própria máquina (`GREATEST(0, expedição − reserva da máquina)`), enquanto o **artigo/KPIs** descontavam a reserva total (`GREATEST(0, Σexpedição − Σreserva)`); quando uma OF reservava de uma máquina sem saldo de expedição (TEAR 31 com 104 pç reservadas e estoque zerado), esse excedente sumia do detalhamento por máquina mas continuava descontado no total — diferença exata de 104 peças. Correção (migration): o excedente de reserva do artigo passa a ser alocado de forma **determinística e sequencial** (ordem por `machine_id`, sem rateio proporcional) sobre a capacidade livre das demais máquinas do mesmo cliente+artigo, e o total do artigo/KPIs passou a ser a soma dos disponíveis por máquina. Assim Σ(máquinas) ≡ artigo ≡ KPIs, cada reserva reduz o disponível na quantidade exata e o palete "em máquina" nunca é consumido por reserva. Verificado no banco: zero divergências entre soma por máquina e total do artigo (kg e rolos) em toda a base. REVOKE `PUBLIC`/`anon` e GRANT `authenticated`/`service_role` reaplicados.
 - **02/08/2026 (Brasília) — Pente fino de segurança: RPCs de Relatórios e Dashboard sem isolamento de empresa:** Auditoria das últimas entregas encontrou 9 funções `SECURITY DEFINER` (`get_dashboard_metrics`, `get_report_kpis`, `get_report_data` (2 sobrecargas), `get_report_by_machine`, `get_report_by_article`, `get_report_by_client`, `get_report_by_shift`, `get_report_evolution`) que **confiavam apenas no parâmetro `p_company_id`**, sem validar o tenant do chamador — qualquer usuário autenticado podia ler produção, faturamento e eficiência de outra empresa passando outro UUID; além disso estavam sem `search_path` fixo. Correção (migration com DO block que reescreve cada função preservando o corpo): guarda `IF public.get_user_company_id() IS DISTINCT FROM p_company_id THEN RETURN; END IF;` injetada logo após o `BEGIN` (retorno vazio para `RETURNS TABLE`, `NULL::json` para `RETURNS json`), `SET search_path TO 'public'`, `REVOKE ALL ... FROM PUBLIC, anon` e `GRANT EXECUTE ... TO authenticated, service_role`. Verificado: 9/9 funções com guarda + `search_path`, `anon` sem execute, consulta com company_id de outra empresa retorna vazio e Dashboard/Relatórios seguem carregando normalmente na empresa do usuário. `tsgo --noEmit` limpo.
  - **02/08/2026 (Brasília) — Estoque Malha (Manual): reserva de OF volta a baixar o disponível na quantidade exata:** Usuário reportou que ao reservar 9 peças (B BASIC/POWER DROP · MALHA 1,20-155 OE · TEAR 01) o disponível caía de 59 para 53 (esperado 50) e, em Bil têxtil · MALHA EXCLUSIVE · TEAR 13, reserva de 11 baixava de 111 para 101. Causa: em `get_manual_stock_estoque` o excedente de reserva de outra máquina (OF 434 reservando 195 pç no TEAR 12, que só tem 65 pç de estoque manual) era **redistribuído proporcionalmente** entre as demais máquinas do mesmo cliente+artigo; como a capacidade livre muda a cada nova reserva, a fatia do rateio se redistribuía e o delta observado ficava menor que o reservado. Correção (migration): removido o rateio proporcional (`spill`/`alloc`). Agora o disponível por máquina é `em_maq + GREATEST(0, expedição − reservado da própria máquina)` e o disponível por artigo/cliente/KPIs é `SUM(em_maq) + GREATEST(0, SUM(expedição) − SUM(reservado))`. Assim toda reserva reduz o disponível exatamente na quantidade reservada, tanto no tear quanto no total do artigo, sem inflar saldo e sem ficar negativo. REVOKE `PUBLIC`/`anon` e GRANT `authenticated`/`service_role` reaplicados.
  - **02/08/2026 (Brasília) — Pente fino OF (auditoria de separação):** Auditoria da entrega de auditoria detalhada da OF. Verificado sem problemas: colunas `separation_started_by/at` e `separation_finished_by/at` criadas e populadas pelas RPCs `start_billing_order_separation` / `launch_billing_order_ready`; `get_billing_orders_list`, `get_billing_order_detail` e `get_billing_order_export` retornam nome/código dos responsáveis e os metadados de cada palete; 1.066 paletes com `created_by` preenchido (zero nulos); todas as RPCs do módulo com `SECURITY DEFINER`, `search_path` fixo e execução apenas para `authenticated`/`service_role`; frontend (`src/pages/BillingOrders.tsx`) e PDF consomem os campos corretos. **Bug encontrado e corrigido:** ao reverter uma OF para **Aberto** (via `revert_billing_order_to_open` ou `edit_billing_order` com `p_revert_to_open`), os campos de separação não eram limpos — a OF voltava para Aberto ainda exibindo "Sepa. inic." e "Sepa. fina." da tentativa anterior, e o PDF repetia essa auditoria obsoleta. Migration: trigger `trg_of_clear_separation_audit_on_open` (BEFORE UPDATE OF status em `billing_orders`, função `_of_clear_separation_audit_on_open` com `search_path` fixo e EXECUTE revogado de `PUBLIC`/`anon`/`authenticated`) zera os 4 campos sempre que o status passa a `open`, cobrindo todos os caminhos de reversão; feito também backfill das OFs abertas com dados residuais. `tsgo --noEmit` limpo.
  - **02/08/2026 (Brasília) — Pente fino Estoque Malha (Manual): reserva de OF deixa de ser presa à máquina:** Auditoria do módulo (2.238 movimentos: zero `pieces`/`weight_kg` negativos, zero `on_machine=true` sem máquina, zero `company_id` nulo, zero saldo em máquina negativo, zero espelho órfão de `stock_movements`). **Bug encontrado:** em `get_manual_stock_estoque` o disponível era calculado por `cliente+artigo+máquina` e a reserva era descontada apenas da expedição daquela máquina, com clamp em zero. Quando a OF reservava mais peças do que a máquina de origem tinha na expedição (74 chaves cliente/artigo/máquina no banco, 18 artigos afetados), o excedente **desaparecia** e o "Disponível" do artigo/cliente/KPIs ficava maior que o estoque real. **Correção (migration):** a reserva passou a ser consolidada no nível do artigo — cada máquina consome primeiro a própria expedição (`LEAST(reservado, expedição)`) e o excedente transborda proporcionalmente para a capacidade livre das demais máquinas do mesmo cliente+artigo, de modo que o total do artigo respeita `em_maq + GREATEST(0, estoque − em_maq − reservado_do_artigo)`; o palete em máquina continua nunca sendo consumido por reserva. Os KPIs ganharam `reservedRolls` e `availableRolls`. `REVOKE PUBLIC/anon` e `GRANT authenticated/service_role` reaplicados; tipo `EstoqueKPIs` atualizado em `src/pages/StockMalhaManual.tsx`. `tsgo --noEmit` limpo.
  - **02/08/2026 (Brasília) — Estoque Malha (Manual): "Em maq." volta a contar em Disp. Rolos:** Usuário reportou (cliente Sul Brasil) artigos com **Em maq. = 7 pç** e **Disp. Rolos = 0**. Causa: em `get_manual_stock_estoque` o disponível era `GREATEST(0, estoque − reservado)`, então uma reserva de OF maior que o estoque da expedição zerava também as peças do palete na máquina — mas reserva de OF só pode consumir a expedição, nunca o palete em máquina. Correção (migration): `availableKg`/`availableRolls` passaram a ser `em_maq + GREATEST(0, estoque − em_maq − reservado)`, garantindo que o palete em máquina sempre componha o disponível (por máquina, por artigo, por cliente e nos KPIs). Defaults da assinatura preservados; REVOKE `PUBLIC`/`anon` e GRANT `authenticated`/`service_role` reaplicados.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) (segurança e concorrência):** Auditoria completa após a recontagem de paletes e os novos cards mobile. Banco com 2.203 movimentos: zero valores negativos, zero `company_id` nulo, zero palete em máquina sem máquina, zero saldo em máquina negativo, zero vínculo entre empresas e 2.137 movimentos espelhados sem duplicidade. **Correções:** (1) removidos `INSERT/UPDATE/DELETE` diretos da tabela `manual_stock_movements` para `authenticated` e as respectivas policies — toda escrita agora passa exclusivamente pelas RPCs protegidas, impedindo adulteração de saldo/autor/tipo pelo cliente; (2) `save_manual_stock_manual_entry` e `save_manual_stock_machine_adjust` agora autorizam escrita somente para perfis `admin`/`expedicao` da empresa ativa, validam que cliente, artigo e máquina pertencem à empresa e que o artigo pertence ao cliente; (3) adicionado lock transacional por empresa+cliente+artigo+máquina para serializar recontagens concorrentes e impedir perda de atualização quando dois usuários salvam o mesmo palete; (4) removido o payload incremental legado da RPC de palete e bloqueada transferência com total zero, evitando zerar o saldo em máquina sem entrada correspondente na expedição; (5) saldos históricos sem máquina, antes ocultos no detalhamento desktop mas visíveis no mobile, agora aparecem consistentemente como **Sem máquina** nos dois layouts; (6) criado trigger de `UPDATE` em `stock_movements`, completando o espelhamento INSERT/UPDATE/DELETE: alterações elegíveis atualizam o registro manual pelo `source_movement_id`, alterações que deixam de ser elegíveis removem o espelho e registros que passam a ser elegíveis são incluídos; a função interna tem `search_path` fixo e execução revogada de `PUBLIC`/`anon`/`authenticated`. Funções continuam com `search_path` fixo, sem execução por `anon`, e leitura permanece isolada por tenant. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): ajuste dos cards mobile:** No card do artigo o bloco "Disponível" deixou de ocupar metade da largura (`flex-1` → `shrink-0`, número de `text-xl` para `text-base`), liberando espaço para os mini-stats Est/Rsv/Em maq., que antes ficavam cortados. No card da máquina foram ocultados "Entradas" e "Saídas OF", restando apenas "Em maq." e "Reservado" em grade de 2 colunas junto do destaque "Disponível". `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Palete na máquina: recontagem substitui (não soma) + cards mobile:** `save_manual_stock_machine_adjust` passou a aceitar `set_pieces`/`set_weight_kg`/`move_all` — a quantidade informada é o TOTAL do palete e a RPC grava apenas o delta (adjust_in on_machine=true quando sobe, adjust_out on_machine=true quando desce); com `move_all=true` o total recontado é lançado integralmente na expedição (adjust_in on_machine=false + adjust_out on_machine=true, entrada antes da saída para não gerar prefixo negativo) e o saldo em máquina zera. O caminho legado add/move continua funcionando. Em `src/pages/StockMalhaManual.tsx` o `MachinePalletModal` agora pré-carrega peças/kg atuais do palete, rótulos viraram 'Recontar palete e manter na máquina' / 'Recontar e lançar o palete para a expedição', removido o bloco 'peças a somar' e bloqueado salvar sem alteração. Cards mobile de artigo e máquina redesenhados: 'Disponível' em destaque (bloco primário, número grande) e demais métricas em mini-stats em grade, sem vazamento lateral.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) (pós UX mobile do modal):** Auditoria das últimas entregas. Banco sem inconsistências: 2.202 movimentos, zero `pieces`/`weight_kg` negativos, zero `on_machine=true` sem `machine_id`, zero `company_id` nulo, RPCs do módulo (`get_manual_stock_bootstrap/estoque/movements`, `save_manual_stock_manual_entry`, `save_manual_stock_machine_adjust`) executáveis só por `authenticated`/`service_role`. **Correções aplicadas (frontend `src/pages/StockMalhaManual.tsx`):** (1) o atalho "Usar quantidade atual do palete" gravava o peso com vírgula (`String(kg).replace('.', ',')`) enquanto o `BrazilianWeightInput` espera valor bruto com ponto — o `parseFloat` truncava a parte decimal (ex.: 12,5 kg virava 12 kg) ao salvar; passou a usar `String(kg)`. (2) O modal "Palete na máquina" ficou fora da melhoria mobile do Lançamento Manual: recebeu o mesmo layout 100vw/100dvh (`h-[100dvh]`, `rounded-none`, corpo rolável, footer fixo) no mobile e `max-w-md` no desktop, campos de Peças/Peso a somar com teclado numérico (`inputMode="numeric"`) e fonte 16px no mobile (`text-base md:text-xs`) para evitar o zoom do iOS, igual ao Lançamento Manual. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) (pós motivo opcional):** Auditoria das últimas entregas do módulo. Banco sem inconsistências: 2.197 movimentos, zero `pieces`/`weight_kg` negativos, zero `on_machine=true` sem `machine_id`, RLS por tenant ativa e RPCs executáveis só por `authenticated`/`service_role`. **Correções aplicadas (migrations):** (1) `save_manual_stock_machine_adjust` ainda exigia `reason_required` (mín. 5 caracteres) enquanto o Lançamento Manual tornou o motivo opcional — o modal "Palete na máquina" quebrava com erro genérico; agora o motivo é opcional e grava o padrão "Ajuste de palete na máquina" (frontend: label "Motivo (opcional)" e validação de 5 caracteres removida em `src/pages/StockMalhaManual.tsx`). (2) `save_manual_stock_manual_entry` validava o saldo do palete na máquina somando **todos os clientes** do mesmo artigo+máquina — permitia saída maior que o saldo real do cliente; passou a usar o cliente efetivo (`COALESCE(client_id, cliente do artigo)`), igual a `save_manual_stock_machine_adjust` e ao agrupamento de `get_manual_stock_estoque`. (3) `get_manual_stock_estoque`: a trava cronológica em zero ordenava por `created_at, id`; na transferência máquina → expedição as duas linhas nascem no mesmo instante e, se a saída viesse antes da entrada, o prefixo ficava negativo e o `GREATEST(0, total − min)` **inflava** o estoque do grupo. A janela passou a ordenar entradas antes das saídas no mesmo instante (`ORDER BY created_at, CASE WHEN d_kg + d_pc >= 0 THEN 0 ELSE 1 END, id`) e a RPC de transferência grava o `adjust_in` da expedição antes do `adjust_out` da máquina. REVOKE/GRANT reaplicados nas 3 funções. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): modal de Lançamento Manual (palete em máquina, motivo opcional, scroll nos selects):** Ao escolher **Cliente + Artigo + Máquina** o modal consulta `get_manual_stock_estoque` (filtrada por cliente/artigo) e exibe um bloco destacado informando o palete já registrado naquela máquina — "Esta máquina já tem palete em máquina: N pç (X kg)" — com o aviso de que lançar no destino **Estoque da expedição** mantém o palete **Em maq.** intacto (não soma nem baixa); quando não há palete, mostra "Nenhum palete em máquina registrado para esta combinação". Com destino "Palete na máquina" há o atalho "Usar quantidade atual do palete". A consulta é invalidada após cada salvamento (`manual-stock-machine-pallet`). **Motivo deixou de ser obrigatório**: label passou a "Motivo (opcional)", validação de 5 caracteres removida no frontend e a RPC `save_manual_stock_manual_entry` grava o texto padrão "Lançamento manual" / "Lançamento manual (palete na máquina)" quando o campo vem vazio (a validação `reason_required` foi removida dessa RPC; as demais RPCs seguem inalteradas). **Scroll nos selecionáveis**: `src/components/SearchableSelect.tsx` passou a limitar a lista por viewport (`max-h-[min(360px,45vh)]`) com barra de rolagem visível (`scrollbar-thin`, sem `scrollbar-hide`), `overflow-x-hidden`, rolagem por toque (`-webkit-overflow-scrolling: touch`) e `onTouchMove` contido — vale para Cliente/Artigo/Máquina e todos os selects do sistema. O `DialogContent` do Lançamento Manual ganhou `max-h-[90vh] overflow-y-auto`. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) (pós "Em maq."):** Auditoria do módulo após a entrega do palete em máquina. Banco verificado sem inconsistências: 2.197 movimentos, zero `pieces`/`weight_kg` negativos, zero `on_machine=true` sem `machine_id`, RPCs do módulo executáveis só por `authenticated`/`service_role`, `manual_stock_movements` na publicação realtime e com índices (`idx_msm_on_machine`, `uq_msm_source_movement`). **Correções aplicadas (migration):** (1) `save_manual_stock_manual_entry` permitia lançar **Saída** com destino "Palete na máquina" sem conferir o saldo do palete — isso deixava "Em maq." negativo (mascarado pelo `GREATEST(0,...)` da RPC de estoque) e tirava peças reais do estoque; agora valida `insufficient_machine_stock` por artigo+máquina e rejeita quantidades negativas; (2) `save_manual_stock_machine_adjust` conferia o saldo da máquina apenas por artigo+máquina, ignorando o cliente — a transferência para a expedição passa a comparar o saldo do **cliente efetivo** (`COALESCE(client_id, cliente do artigo)`), igual ao agrupamento de `get_manual_stock_estoque`, e resolve o cliente pelo artigo quando o payload vem sem `client_id`; (3) `get_manual_stock_movements` não retornava `on_machine`, então a aba **Movimentações** exibia lançamentos de palete na máquina idênticos aos da expedição (parecendo duplicidade) — o campo passou a ser retornado e o frontend mostra o badge **"Em máq."** ao lado do tipo (desktop e mobile). Frontend também ganhou validação de **peso** acima do saldo em máquina no modal "Palete na máquina" e mensagem amigável para `insufficient_machine_stock` no Lançamento Manual. REVOKE/GRANT reaplicados nas 3 funções. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): palete em máquina ("Em maq."):** Novo controle para evitar duplicidade entre a contagem do palete na máquina e o mesmo palete quando chega na expedição. Migration: coluna `manual_stock_movements.on_machine boolean NOT NULL DEFAULT false` (+ índice `idx_msm_on_machine`); `save_manual_stock_manual_entry` passa a aceitar `on_machine` (exige máquina quando true); nova RPC `save_manual_stock_machine_adjust(p_payload)` (SECURITY DEFINER, tenant-checked, execute só para authenticated/service_role) que (a) adiciona peças/kg ao palete da máquina (`adjust_in` com `on_machine=true`) e/ou (b) transfere peças/kg da máquina para a expedição gravando `adjust_out on_machine=true` + `adjust_in on_machine=false` (soma zero no total, apenas migra de "Em maq." para o estoque da expedição), com validação `insufficient_machine_stock`. `get_manual_stock_estoque` ganhou `machineKg`/`machineRolls` por máquina, artigo, cliente (`totalMachineRolls`) e KPI, sempre limitado ao estoque físico do grupo — `Disp. Rolos` continua sendo o total (máquina + expedição), sem duplicar. Frontend (`src/pages/StockMalhaManual.tsx`): no modal "Lançamento Manual" há o seletor **Destino** (Estoque da expedição / Palete na máquina); a tabela desktop ganhou a coluna "Em maq." depois de "Disponível kg" e um botão "Palete" na linha de cada máquina; no mobile o card do artigo mostra o badge "Em maq." e cada máquina tem o botão "Ajustar palete da máquina". O novo modal "Palete na máquina" permite adicionar peças mantendo na máquina ou lançar peças (com adição opcional antes) para a expedição. Auditoria: ação `STOCK_MANUAL_MACHINE_PALLET`. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Pente fino pós-entregas (status de máquina ao finalizar ordens):** Auditoria das últimas entregas do mestre.md. Verificado sem erro: `manual_stock_movements` com 2.187 linhas, zero `pieces`/`weight_kg` negativos, zero `source_movement_id` órfão, zero `reserve`/`release` sem OF; `ot_photos` e `collect_photos` 100% arrays JSON válidos; buckets `oc-photos`, `of-photos` e `freight-photos` privados; apenas 1 função SECURITY DEFINER executável por `anon` (`get_company_public_by_slug`, necessária no login); frontend do Estoque Manual consistente com o novo `availableRolls` (sem desconto duplicado de reservas). **Correção aplicada (bug de dados):** `finalize_maintenance_order` e `finalize_article_change_order` gravavam `machines.status = 'ativa'` incondicionalmente — ao finalizar uma OM/OC/OT numa máquina que ainda tinha uma OE (ou outra ordem) em curso, a máquina voltava para "ativa" e sumia de Ordens/Dashboard (encontradas 5 máquinas nessa condição: TEAR 04, 06, 09, 12 e 15). Criada `public.next_machine_status(p_machine_id, p_exclude_mo, p_exclude_ac)` (STABLE, SECURITY DEFINER, search_path fixo, execute só para authenticated/service_role), que retorna o tipo da ordem ainda `em_curso` na máquina, ou `troca_artigo` se houver OT em `troca_fio_em_curso`/`em_regulagem`, ou `ativa`. As duas RPCs de finalização (e o `machine_logs` aberto na finalização de OM) passaram a usar essa função em vez do literal `'ativa'`. Backfill executado: máquinas divergentes reajustadas (zero divergências após a correção). `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): Disponível desconta reservas de OF:** `get_manual_stock_estoque` calculava `availableKg/availableRolls` como cópia de `stockKg/stockRolls`, ignorando as reservas — ao reservar um palete em uma OF, "Rolos reservados" subia mas "Disp. Rolos" continuava igual (ex.: MALHA NEO SOUL com 96 rolos reservados e 445 disponíveis sem desconto). Correção na CTE `agg3`: `available_kg = GREATEST(0, stock_kg − reserved_kg)` e `available_rolls = GREATEST(0, stock_rolls − reserved_rolls)`, propagando para artigo, cliente, quebra por máquina e KPIs. Como `res` só considera OFs cujo status não é `collected`/`cancelled`, ao cancelar a OF ou excluir o palete as peças voltam automaticamente ao disponível; na coleta a reserva vira saída física. REVOKE/GRANT reaplicados (authenticated/service_role). Sem mudança de tabelas, triggers, RLS ou frontend.
  - **01/08/2026 (Brasília) — Pente fino de segurança (pós-entregas OT/OF/OE):** Auditoria das últimas entregas. Verificado sem erro: coluna `article_change_orders.ot_photos jsonb NOT NULL DEFAULT '[]'` criada e retornada pela RPC `get_article_change_orders_list` (usa `o.*`); buckets `oc-photos` e `of-photos` privados com policies de tenant (`storage.foldername(name)[1] = get_user_company_id()`) em SELECT/INSERT/UPDATE/DELETE; policies de tenant completas em `article_change_orders` (o UPDATE de `ot_photos` após a conclusão passa pela RLS); nenhum GRANT para `anon` em tabelas do schema public. **Correção aplicada (segurança):** 124 funções `SECURITY DEFINER` do schema public (RPCs de OF, OFR, mecânica/OT/OM/OC/OE, estoques, relatórios, notas e trigger functions) estavam executáveis por `PUBLIC`/`anon` — qualquer chamada com a chave pública, sem login, atingia o corpo da função. Migration revoga `ALL ... FROM PUBLIC, anon` e reaplica `GRANT EXECUTE ... TO authenticated, service_role` em todas elas, mantendo pública apenas `get_company_public_by_slug` (necessária na tela `/slug-empresa/login`). Linter caiu de 281 para 151 ocorrências; as remanescentes são pré-existentes (search_path mutável, buckets públicos de logo, policy de criação de empresa no cadastro e policy `ALL true` restrita a `service_role` em `iot_device_logs`). Confirmado que 0 funções perderam acesso para `authenticated`. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — OT: até 3 fotos na conclusão + "Ver fotos" em Concluídas:** No modal "Revisão da peça — OT #NNN" (`src/components/mecanica/ArticleChangeOrdersTab.tsx`, aba Em Acompanhamento) foi adicionada a seção "Fotos da conclusão (opcional)": câmera (`capture=environment`) ou galeria, limite de 3 imagens e 8 MB por arquivo, preview e descrição por foto. O envio reutiliza o padrão do sistema — `compressImage` (`@/lib/imageCompression`) + overlay `uploadProgress` (compressing → uploading → finalizing), upload para o bucket privado `oc-photos` em `${company_id}/${ot_id}/${uuid}.ext` — e ocorre após a RPC `finalize_article_change_order` retornar sucesso; em caso de falha as imagens enviadas são removidas (rollback) e a OT permanece concluída com toast de aviso. Migration: coluna `article_change_orders.ot_photos jsonb NOT NULL DEFAULT '[]'` (a RPC `get_article_change_orders_list` usa `o.*`, então já retorna o campo). No card, quando há fotos, aparece bloco roxo destacado com badge "N foto(s) anexada(s)" e botão sólido "Ver fotos" que abre o novo `OTPhotosModal` (signed URLs de 1h, grid com descrição, autor e data). `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Mecânica (OM/OC/OE/OT): hierarquia do card e carregamento completo:** Nos cards de ordens (`src/components/mecanica/MaintenanceOrdersTab.tsx` e `ArticleChangeOrdersTab.tsx`) o número da ordem (OM/OC/OE/OT #NNN) deixou de ser o elemento mais destacado (era `text-lg font-bold`): agora é `text-xs sm:text-sm font-semibold` em tom secundário (destructive só na OC), e o **nome da máquina** passou a ser o destaque principal (`text-xl font-bold`, com `break-words`). Além disso, em conexões lentas o card não mostra mais dados parciais (descrição sem máquina): a renderização só ocorre quando as ordens **e** os dados da empresa (`useSharedCompanyData().loading`) terminaram de carregar; enquanto isso é exibido o novo `src/components/mecanica/OrderCardSkeleton.tsx` — cards placeholder com spinner e a mensagem "Carregando dados da {OM|OC|OE|OT}…". Sem migration. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — OF: fotos na coleta:** Em "Pronto para coleta", o botão de coleta passou a abrir o modal `OfCollectPhotosModal` (`src/components/billing/OfCollectPhotosModal.tsx`), que permite anexar até 3 fotos (câmera `capture=environment` ou galeria, 8 MB por arquivo, descrição opcional) antes de confirmar. O envio reutiliza o padrão do sistema: `compressImage` (`@/lib/imageCompression`) + overlay `uploadProgress` (compressing → uploading → finalizing), upload para o novo bucket privado `of-photos` em `${company_id}/${order_id}/${uuid}.ext`, com rollback das imagens caso a coleta falhe. Migration: coluna `billing_orders.collect_photos jsonb NOT NULL DEFAULT '[]'` + policies de tenant em `storage.objects` para o bucket `of-photos` (leitura/escrita/exclusão restritas ao `get_user_company_id()`). Na aba **Coletadas** o card exibe bloco roxo com badge "N fotos anexadas" e botão "Ver fotos", que abre `OfPhotosViewerModal` com signed URLs (1h). `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual) (pós-realtime):** Auditoria completa do módulo. Banco verificado sem inconsistências: 2.156 movimentos, zero `company_id` nulo, zero `billing_order_id`/`article_id`/`machine_id` órfãos, zero `pieces`/`weight_kg` negativos, zero duplicidade real (as 358 repetições por OF+artigo+máquina+tipo são paletes distintos — nenhuma linha idêntica), zero `source_movement_id` órfão ou repetido, e **todas as OFs coletadas após o baseline (27/07 19:30) possuem linha `out` espelhada** (as 359 sem `out` são anteriores ao baseline zerado, por decisão de projeto). Permissões conferidas: tabela sem acesso a `anon`, RLS ativa com 4 policies, todas as RPCs do módulo executáveis apenas por `authenticated`/`service_role`; triggers `trg_mirror_of_to_manual_stock` / `trg_mirror_of_delete_to_manual_stock` ativos. Correções aplicadas: (1) criado índice único parcial `uq_msm_source_movement` em `source_movement_id` — antes nada impedia que um reprocessamento do trigger espelhasse duas vezes a mesma saída de OF (duplicando baixa de estoque); (2) `get_manual_stock_estoque` reescrita sem as colunas mortas `dummy`, `dummy2` e `mx_kg` do CTE `stock` (expressões inúteis do tipo `MAX(pfx_kg)*0 + SUM(0)`), mantendo idêntico o resultado (saldo cronológico travado em zero e reservas só de OFs ativas); GRANT/REVOKE reaplicados. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual) em tempo real:** Realtime reforçado no módulo. Migration: `REPLICA IDENTITY FULL` em `billing_orders` e `billing_order_pallets` e inclusão de `billing_order_pallets` na publicação `supabase_realtime` (`manual_stock_movements` já estava publicada com replica identity full). Em `src/pages/StockMalhaManual.tsx` o canal passou a escutar também `billing_orders` e `billing_order_pallets` (filtrados por `company_id`), pois reservas/baixas de OF alteram o saldo sem gerar linha em `manual_stock_movements` em todos os casos. Adicionados: nome de canal único por aba (evita colisão entre usuários/abas), debounce de 250 ms nas invalidações e refetch em `visibilitychange` (reconecta ao voltar para a aba no mobile/PWA). Assim, entradas manuais e mudanças de OF aparecem para todos os usuários conectados sem F5.
  - **01/08/2026 (Brasília) — Pente fino Estoque Malha (Manual):** Auditoria das entregas recentes do módulo. (1) **Segurança (corrigido):** `public.manual_stock_movements` tinha GRANT completo (`arwdDxtm`) para o papel `anon` e todas as RPCs do módulo (`get_manual_stock_estoque`, `get_manual_stock_bootstrap`, `get_manual_stock_movements`, `save_manual_stock_manual_entry` — esta última SECURITY DEFINER e de escrita) estavam executáveis por `PUBLIC`/`anon`. Embora as policies sejam `TO authenticated` e as funções barrem chamadas sem empresa (`get_user_company_id()` nulo), a superfície foi removida: `REVOKE ALL ... FROM anon/PUBLIC` na tabela e nas funções (inclusive nas trigger functions `mirror_of_*`), com GRANT reaplicado apenas para `authenticated`/`service_role`. (2) **Inconsistência de KPIs (corrigida):** os lançamentos manuais reais são feitos só em peças (`adjust_in`/`adjust_out` com `weight_kg = 0` no banco), então os KPIs em kg exibiam 0,00 kg enquanto havia estoque real em rolos (e "Reservado" mostrava kg vindos das OFs espelhadas, gerando leitura contraditória do tipo Est 0,00 kg / Rsv 590 kg). Os quatro KPIs passaram a exibir **peças como valor principal** (somadas de `entradaRolls`/`deliveredRolls`/`reservedRolls`/`availableRolls` via novo `useMemo totalRolls`) com o kg em linha secundária. (3) Verificado sem problemas: overload único de `get_manual_stock_estoque` (uuid, text, uuid, uuid); triggers `trg_mirror_of_to_manual_stock` / `trg_mirror_of_delete_to_manual_stock` ativos em `stock_movements`; `REPLICA IDENTITY FULL`; policies por `get_user_company_id()`; zero movimentos com `billing_order_id` órfão. `tsgo --noEmit` limpo.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): mobile sem vazamento e valores em peças:** Em `src/pages/StockMalhaManual.tsx` o cabeçalho do card por cliente (fechado) deixou de vazar no mobile: `CardHeader` agora é `flex-col` no mobile (`md:flex-row`), com `overflow-hidden`, nome do cliente `truncate min-w-0`, botão de PDF `shrink-0` e os totais em `flex-wrap` com `whitespace-nowrap`. Além disso, a visão mobile passou a exibir **peças (pç)** no lugar de kg: os totais do cliente (Entradas/Reservado/Disponível) usam a soma de `entradaRolls`/`reservedRolls`/`availableRolls`, os badges do artigo (`Est`/`Rsv`/`Disp`) usam `stockRolls`/`reservedRolls`/`availableRolls` e o detalhe por máquina (Entradas, Saídas OF, Reservado, Disponível) usa os campos `*Rolls`. Nome do artigo/máquina com `break-words` para não estourar. Desktop e RPCs inalterados.
  - **01/08/2026 (Brasília) — Fix Estoque Malha (Manual) vazio (overload ambíguo da RPC):** A página parou de listar dados porque existiam DUAS versões de `public.get_manual_stock_estoque` com a mesma aridade e mesmos nomes de parâmetro em ordem diferente — `(p_company_id, p_month, p_client_id, p_article_id)` (nova, saldo cronológico travado em zero) e `(p_company_id, p_client_id, p_article_id, p_month)` (antiga). Como `src/pages/StockMalhaManual.tsx` chama por argumentos nomeados, o PostgREST não resolvia a função (função ambígua) e a tela ficava vazia. Correção: `DROP FUNCTION public.get_manual_stock_estoque(uuid, uuid, uuid, text)` (versão antiga, que ainda filtrava saldo e reservas por mês) + GRANT EXECUTE reaplicado na versão vigente para authenticated/service_role. Consequência: o filtro de mês afeta somente "Entradas manuais" e "Entregue (OF coletadas)"; `stockKg/stockRolls`, `availableKg/availableRolls` e reservas são sempre all-time — o estoque real atual não muda conforme o período. Sem alteração de tabelas, triggers, RLS ou frontend.
  - **01/08/2026 (Brasília) — Estoque Malha (Manual): saldo refeito do zero (sem negativo invisível) + reservas saem na coleta:** A RPC `get_manual_stock_estoque` foi reescrita. (1) O saldo (`stockKg/stockRolls` = `availableKg/availableRolls`) deixou de ser `entradas − saídas` (que gerava dívida oculta: uma OF coletada sem estoque manual criava saldo negativo que precisava ser "reposto" antes de novas entradas aparecerem — caso Bil têxtil: 827 peças lançadas exibindo só 223). Agora o saldo é um **acumulado cronológico travado em zero** por (cliente, artigo, máquina): uma saída só consome o que existia naquele instante. Implementado sem loop, via janela `SUM(...) OVER (PARTITION BY ... ORDER BY created_at, id ROWS UNBOUNDED PRECEDING)` e a identidade `saldo_final = total − LEAST(0, MIN(prefixo))`. Resultado validado: Bil têxtil passa de 0/223 para 687 rolos disponíveis (total bruto seria −485). (2) `reservedKg/reservedRolls` passou a considerar apenas OFs cujo status **não** é `collected` nem `cancelled` e deixou de sofrer o filtro de mês (reserva em um mês + release/coleta no mês seguinte mantinha a reserva presa). Ao marcar a OF como coletada, as peças saem imediatamente de "Rolos reservados". (3) Entradas/Entregues continuam respeitando o filtro de mês; o saldo é sempre all-time (correto por natureza). GRANT EXECUTE reaplicado para authenticated/service_role. Sem mudança de tabelas, triggers ou RLS.
  - **28/07/2026 (Brasília) — Pente fino (fotos da finalização de OE):** Auditoria da entrega de fotos na finalização de OE. Banco verificado: única OE existente (#001) está `em_curso`, numeração isolada (`oe_number` sem colidir com `om_number`/`oc_number`); `finalize_maintenance_order` fecha o machine_log, reativa a máquina (`status = ativa`) e é idempotente (`already = true` sem duplicar itens/push); nenhuma migration necessária. Linter do banco sem novas ocorrências ligadas a OE/`oc_photos` (apenas warnings pré-existentes de search_path e buckets públicos). Correção aplicada: o modal "Finalizar OE" só limpava `finishPhotoDrafts` no botão "Fechar" — fechar por ESC/overlay deixava object URLs vivos (vazamento de memória); o `onOpenChange` agora chama `clearFinishPhotoDrafts()` e ignora o fechamento enquanto `finishing` está ativo, evitando abortar visualmente um upload em andamento. `tsgo --noEmit` limpo.
  - **28/07/2026 (Brasília) — OE: até 2 fotos na finalização:** No modal "Finalizar OE" (`src/components/mecanica/MaintenanceOrdersTab.tsx`) foi adicionada a seção "Fotos da finalização (opcional)", exclusiva para `manutencao_eletrica`, com câmera (`capture=environment`) ou galeria, limite de 2 imagens, preview, descrição por foto e limite de 8 MB por arquivo. O envio reutiliza o padrão do sistema: `compressImage` (`@/lib/imageCompression`) + overlay `uploadProgress` (compressing → uploading → finalizing), upload para o bucket `oc-photos` em `${company_id}/${order_id}/${uuid}.ext` e append no array `oc_photos` da ordem (preservando fotos já anexadas na criação). Em caso de falha, as imagens já enviadas são removidas do bucket (rollback) e a OE permanece finalizada com toast de aviso. As fotos aparecem no bloco roxo "Ver fotos" do card e no modal de relatório. Sem migration.
  - **28/07/2026 (Brasília) — OE: descrição e fotos em destaque:** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` os cards de OE (`manutencao_eletrica`) passam a exibir a Descrição do problema em bloco destacado (borda/fundo amarelo elétrico + ícone Zap), como já acontecia na OC (vermelho). O indicador de fotos deixou de ser exclusivo da OC: agora OC e OE mostram um bloco roxo com badge "N fotos anexadas" e botão sólido "Ver fotos" que abre o modal de relatório com as imagens. O título do modal de relatório passou a usar `labelOf`/`displayNumber` (antes rotulava OE como "OM #—"). Sem migration.
  - **28/07/2026 (Brasília) — Pente fino das últimas entregas (OE / SearchableSelect):** Auditoria completa das entregas de hoje. Banco verificado: `maintenance_orders_type_check` agora aceita `manutencao_eletrica` (confirmado via pg_constraint); trigger `assign_om_number` numera OE em sequência própria (`oe_number`) zerando `om_number`/`oc_number`; `escalate_oc_to_oe` fecha o machine_log da OC, abre log `manutencao_eletrica`, atualiza `machines.status` e finaliza a OC com relatório automático; `finalize_maintenance_order` reativa a máquina e cobre OE sem ramo específico faltante; enum `machine_status` já inclui `manutencao_eletrica`; `get_maintenance_orders_list` devolve `row_to_json` (inclui `oe_number`). OE existente no banco (#001, aberto) consistente. Frontend: `displayNumber`/`labelOf`/filtros de aba, AppSidebar, MobileBottomNav, Ordens.tsx, Machines.tsx e omReportPdf.ts todos já tratam `manutencao_eletrica`. Correções aplicadas: (1) `LiveTimer` em `src/components/mecanica/MaintenanceOrdersTab.tsx` convertido para `forwardRef` — eliminava o warning de console "Function components cannot be given refs" ao ser usado dentro de Badge; (2) confirmado que apenas o modal de OM/OC/OE passa `className` para o SearchableSelect, então a mudança de layout do popover (flex/overflow-hidden/max-w) não afeta os demais 20+ usos. Sem migration nesta rodada. `tsgo --noEmit` limpo.
  - **28/07/2026 (Brasília) — OE sem indicadores preventivos:** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` o bloco de urgência ("Dias p/ próxima" / "Kg restantes") deixou de ser exibido nos cards de OE (`type = manutencao_eletrica`), assim como já ocorria em OC — ordem elétrica não segue meta preventiva do Calendário. Sem migration.
  - **28/07/2026 (Brasília) — OE: tipo em branco e lista de máquinas vazando:** No modal "Nova OE — Ordem Elétrica" (`src/components/mecanica/MaintenanceOrdersTab.tsx`) o Select de Tipo ficava vazio porque `OM_TYPE_LABELS` não inclui `manutencao_eletrica` (não havia SelectItem correspondente ao valor do form). Agora, quando `form.type === 'manutencao_eletrica'`, o Select renderiza o item "Manutenção Elétrica" e fica desabilitado (como já ocorre na OC). Em `src/components/SearchableSelect.tsx` o dropdown de máquinas transbordava o card: PopoverContent ganhou `flex flex-col overflow-hidden max-w-[calc(100vw-2rem)]`, o header de busca `shrink-0`, a lista `min-h-0 flex-1` (respeita max-h externo, ex. `max-h-[50vh]`) e os rótulos passaram de `whitespace-nowrap` para `truncate`. Sem migration.
  - **28/07/2026 (Brasília) — Correção: erro ao criar OE:** A constraint `maintenance_orders_type_check` ainda listava apenas `manutencao_preventiva`, `manutencao_corretiva`, `troca_artigo` e `troca_agulhas` — qualquer INSERT de OE (`type = manutencao_eletrica`) era rejeitado pelo Postgres, gerando o toast "Erro ao criar OE". Migração recria a constraint incluindo `manutencao_eletrica`. Em `src/components/mecanica/MaintenanceOrdersTab.tsx` os toasts de erro de criação/atualização passam a exibir `error.message` para diagnóstico futuro.
  - **27/07/2026 (Brasília) — KPI Estoque Malha (Manual) trocado para Rolos disp.:** Em src/pages/StockMalhaManual.tsx o card "Disponível" (que mostrava availableKg em kg) foi trocado por "Rolos disp.", exibindo a soma total de availableRolls agregada de todos os grupos/artigos (reduce sobre estoque.groups[].articles[].availableRolls). Cor mantém regra (destructive se <0, success caso contrário). Nenhuma mudança de RPC.
  - **27/07/2026 (Brasília) — PDFs Estoque Malha (Manual): cabeçalho sem "(MANUAL)" e colunas alinhadas:** Em `src/pages/StockMalhaManual.tsx` removido "(MANUAL)" dos títulos ("ESTOQUE DE MALHA POR ARTIGO", "POR CLIENTE — GERAL", "POR CLIENTE — POR MÁQUINA"). Reordenadas colunas: PDF por artigo agora `MÁQUINA, ARTIGO, DISP. ROLOS` (antes `MÁQUINA, DISP. ROLOS, ARTIGO`); PDF por cliente/por máquina agora `MÁQUINA, ARTIGO, DISP. ROLOS` (antes `ARTIGO, MÁQUINA, DISP. ROLOS`) com reorder dos cells antes do autoTable. Alinhamento normalizado (Máquina/Artigo à esquerda, Disp. Rolos centralizado) via columnStyles + didParseCell no head. Linha TOTAL usa colSpan: 2. Sem mudanças de dados/RPC.
  - **27/07/2026 (Brasília) — Estoque Malha (Manual): modal de lançamento não fecha e limpa só peças/kg:** No modal "Lançamento Manual — Estoque Malha (Manual)" (`src/pages/StockMalhaManual.tsx`), após salvar com sucesso o modal permanece aberto e apenas os campos Peças e Peso (kg) são zerados; Tipo, Cliente, Artigo, Máquina e Motivo ficam preservados para agilizar lançamentos sequenciais do mesmo artigo/máquina. `onSaved()` continua invalidando as queries de estoque/movimentações para atualizar a tela em tempo real.
  - **27/07/2026 (Brasília) — Fix Estoque Malha (Manual) entrada 100% do zero:** Usuário reportou que entrada manual de 140 peças no artigo `RIBANA 2X1 ALGODÃO` / cliente Silk aparecia como apenas 6 disponíveis porque a RPC `get_manual_stock_estoque` ainda descontava reservas antigas de OFs (`reserve−release`) do campo disponível. Corrigida a RPC para manter reservas antigas visíveis em `reservedKg/reservedRolls`, mas calcular `availableKg/availableRolls` somente a partir do estoque físico manual (`adjust_in−adjust_out−out+in`), com clamp ≥0. Assim, o Estoque Malha (Manual) realmente inicia do zero: toda entrada manual nova contabiliza 100% no disponível, e apenas OF coletada futura baixa esse estoque. Sem mudança de tabela/RLS; GRANT EXECUTE reaplicado para authenticated/service_role.
  - **27/07/2026 (Brasília) — OT: editar em Aberto:** Em Mecânica → OT, aba Aberto, adicionado botão "Editar" (Admin/Líder/Líder Noite) que reabre o modal de criação em modo edição — permite alterar máquina, artigo atual, próximo artigo, fitas de fio, elastano e observação. Modal NewOTModal ganhou prop opcional editing?: OT; quando presente prefill dos campos e do array de fitas/elastano a partir de o.yarns, o UPDATE substitui o cabeçalho em article_change_orders e faz DELETE+INSERT idempotente em article_change_yarns, a checagem de OT duplicada por máquina ignora o próprio editing.id, e não dispara push nem re-preenche artigo pela máquina. Sem migration. tsgo --noEmit limpo.
  - **27/07/2026 (Brasília) — Pente fino Estoque Malha (Manual) baseline zerado:** Auditoria das duas entregas de hoje (delete dos 446 registros  históricos de Trama Certa = 626.391 kg + clamp de / a ≥0 na RPC get_manual_stock_estoque). Verificado com aggregate direto: entrada=0, delivered=0, stock=0, reserved=92.149 kg (líquido reserve−release das OFs em Pronto), available=0 — exatamente o baseline pedido. Frontend (src/pages/StockMalhaManual.tsx) já trata < 0 apenas como estilo (badge Alerta / cor destructive); com valores clampados a 0 o caminho vermelho fica dormente sem quebrar. Triggers mirror_of_to_manual_stock/mirror_of_delete_to_manual_stock intactos: novas OFs coletadas voltam a gerar  normal e o estoque passa a evoluir a partir do zero. Nenhum outro drift detectado nas entregas recentes.
  - **27/07/2026 (Brasília) — Fix Estoque Malha (Manual) — reservas antigas de OFs:** Usuário reportou "COTTON FIO 30/1 PENT" do cliente Evandro com 140 peças coletadas ainda constando como reservado. Diagnóstico: o mirror trigger `mirror_of_to_manual_stock` só espelha INSERTs novos em `stock_movements`; as reservas geradas antes de o módulo Manual existir nunca foram copiadas para `manual_stock_movements`, mas as liberações (`release`) e saídas (`out`) emitidas por `collect_billing_order` a partir da instalação passaram a ser espelhadas — deixando `reserve − release` permanentemente negativo (ex.: 0 − 140 = −140 peças). Auditoria: 2.231 movimentações faltando espelhamento (empresas que usam o módulo — hoje só Trama Certa). Correção em duas frentes: (1) backfill idempotente inserindo em `manual_stock_movements` todo `stock_movements` (reserve/release/out e in com billing_order_id) sem `source_movement_id` correspondente, restrito às empresas com linhas na tabela manual; (2) blindagem em `get_manual_stock_estoque` com `GREATEST(0, ...)` sobre `reserved_kg`/`reserved_rolls` por (client,article,machine) — impede que divergências históricas futuras produzam reservas negativas. Sem mudança de UI, hook ou tipos.
  - **26/07/2026 (Brasília) — Pente fino sidebar sem piscada:** Auditoria da entrega anterior detectou regressão real no `refreshData` (botão Atualizar do header): `loadAllData` chamava `setSettingsLoaded(false)` incondicionalmente no início, e como refresh não é initial (`isInitial=false` → LoadingScreen não reaparece), o sidebar caía para `items=[]` durante toda a janela do refresh e voltava depois — exatamente a piscada que a entrega original corrigia, só que reaberta no fluxo de refresh. Corrigido restringindo o reset a `if (!hasLoadedOnceRef.current) setSettingsLoaded(false)`: no primeiro load o gate continua ativo (LoadingScreen esconde a UI), em refresh `settingsLoaded` permanece `true` e `enabledNavItems` só é sobrescrito no final quando os novos dados chegam. tsgo --noEmit limpo.
  - **26/07/2026 (Brasília) — Sidebar sem piscada de itens após load:** O `AppSidebar` fazia fetch próprio de `company_settings.enabled_nav_items` fora do fluxo de load da empresa — enquanto `enabledNavItems` era `null`, o memo `items` mostrava TODA a lista `allItems`; quando a resposta chegava (depois da LoadingScreen sair), o filtro rodava e itens desabilitados sumiam gerando piscada visual. Corrigido movendo `enabled_nav_items` para dentro do `useCompanyData`: passou a ser lido junto com o `csRes` do carregamento pesado (já era `select('*')`) e ganhou também uma query dedicada nos branches `skipHeavyLoad` (freteiro) e `liderFreteLightLoad` para que roles leves também tenham o filtro pronto no primeiro render. Adicionados `enabledNavItems` e `settingsLoaded` ao retorno do hook/`CompanyDataContext`. O `AppSidebar` agora lê ambos via `useSharedCompanyData`, removeu o fetch local e o `useMemo items` retorna `[]` enquanto `settingsLoaded=false` — como a LoadingScreen só sai depois de `setLoading(false)` (que acontece após `setSettingsLoaded(true)` no finally do `loadAllData`), o sidebar aparece já filtrado. Sem mudança de UI/estado para as demais páginas.
  - **26/07/2026 (Brasília) — /admin: NAV_ITEMS completo:** A lista de módulos toggláveis por empresa/usuário em /admin (src/pages/Admin.tsx) estava desatualizada — faltavam Estoque Malha, Estoque Malha (Manual), Ordem de Faturamento (OF), Ordem de Frete (OFR) e Notas Fiscais (Clientes), além de Vendas de Resíduos com rótulo antigo. Como consequência, admins não conseguiam desabilitar esses módulos do sidebar para uma empresa/usuário (AppSidebar filtra por enabled_nav_items[key], mas a UI não expunha os switches). Adicionadas todas as chaves faltantes na ordem do AppSidebar. Ordens e sub-itens de Mecânica (OM/OC/OE/OT) continuam sem toggle próprio — Ordens é dinâmico (só aparece quando há ordens em execução) e os sub-itens seguem o toggle do pai Mecânica.
  - **26/07/2026 (Brasília) — OFR sem cap de 1000 registros:** PostgREST limita cada resposta a 1000 linhas. Em `src/hooks/useFreightOrders.ts` a query principal de `freight_orders` (usada por TODAS as abas de OFR — Aberto Prioritário, Aberto, Em Rota, Finalizados, Cancelados e Relatórios) e o cálculo de `nextOfrNumber` truncavam silenciosamente após 1000 OFRs, escondendo históricos antigos dos Relatórios e podendo reutilizar número de OFR quando o total ultrapassasse 1000. Ambos foram reescritos para paginar via `.range(from, from+999)` em loop até esgotar, preservando o join complexo (freighter/cost_company/items/photos/edit_photos/profiles) e a ordenação `created_at desc, id desc` para determinismo entre páginas. Sem mudança de UI, tipos ou contrato do hook.
  - **26/07/2026 (Brasília) — Pente fino últimas atualizações (26/07):** Auditoria completa das entregas mais recentes. Verificado: (a) Estoque Malha (Manual) — GRANTs em manual_stock_movements (relacl confirma authenticated=arwdDxtm), REPLICA IDENTITY FULL, publicado em supabase_realtime, policies por get_user_company_id(), triggers mirror_of_to_manual_stock/mirror_of_delete_to_manual_stock corretos, RPCs bootstrap/estoque/movimentacoes/save_manual_entry batem com src/pages/StockMalhaManual.tsx. (b) iot_device_logs — GRANTs efetivos, REPLICA IDENTITY FULL, na publication, policy SELECT reescrita para company_id = get_user_company_id(), IotDevicesManager.tsx consume corretamente (select + canal por device_id). (c) OE (Ordem Elétrica) — enum manutencao_eletrica presente em types/machine.ts/omReportPdf/MaintenanceOrdersTab/Machines/AppSidebar/MobileBottomNav; RPC escalate_oc_to_oe consumido. (d) OF Fases 1–4 — RPCs e hook (useBillingOrders) consistentes, billing_orders em realtime, edit_billing_order preserva paletes com fallback quando chaves mudam. Resultado: nenhum bug novo real; tsgo --noEmit limpo; sem migração adicional necessária.
  - **26/07/2026 (Brasília) — Fix real do modal Logs IoT para Felipe Malharia / TEAOR 01:** Diagnóstico com dados reais mostrou que o dispositivo estava online e gravando normalmente: TEAOR 01/TEAR 01 tinha `last_seen_at` há segundos, 8.949 leituras em `machine_readings` e 100 logs recentes em `iot_device_logs`. O problema não era webhook nem firmware: a policy de leitura dos logs comparava `profiles.id = auth.uid()`, mas `auth.uid()` é o `profiles.user_id` (ID de autenticação), então nenhum usuário autenticado enxergava as linhas mesmo com logs existentes. Corrigida a policy `Users can view logs of their company IoT devices` para `company_id = public.get_user_company_id()`, reaplicados GRANTs efetivos para `authenticated`/`service_role`, mantidos `REPLICA IDENTITY FULL` e publication realtime. Resultado esperado: o modal Logs da empresa Felipe Malharia agora lista imediatamente os 100 últimos registros do TEAOR 01 e continua recebendo novos eventos ao vivo.
  - **26/07/2026 (Brasília) — Fix modal Logs de Dispositivos IoT vazio:** public.iot_device_logs tinha RLS + policies (view por company_id via profiles + service_role ALL) mas ZERO GRANTs na tabela — a query SELECT do modal em IotDevicesManager.tsx era barrada pelo authenticated antes da RLS, então o modal ficava vazio mesmo com dispositivo online inserindo linhas via edge function. Aplicado GRANT SELECT/INSERT/UPDATE/DELETE ON public.iot_device_logs TO authenticated e GRANT ALL TO service_role. Adicionado REPLICA IDENTITY FULL e inclusão na publication supabase_realtime (a tabela ainda não estava publicada) para que o canal em tempo real com filtro device_id=eq.… realmente entregue os novos logs enquanto o modal está aberto. Sem alteração de UI/frontend — o hook de subscription no componente já espera esse contrato. Confirmado que existem 52 linhas em uma company real de teste, agora visíveis.
  - **26/07/2026 (Brasília) — Pente fino Estoque Malha (Manual):** Auditoria completa. 3 bugs reais corrigidos: (1) tabela public.manual_stock_movements sem GRANT para authenticated/service_role — as RPCs SECURITY DEFINER mascaravam a falha em leitura/escrita mas o canal Realtime (postgres_changes filter company_id=eq…) não entregava eventos porque exige SELECT direto na tabela; corrigido com GRANT SELECT/INSERT/UPDATE/DELETE para authenticated e GRANT ALL para service_role. (2) REPLICA IDENTITY FULL não estava setado; sem ele o filtro por company_id no canal Realtime falha silenciosamente (padrão OFR-realtime); ALTER TABLE ... REPLICA IDENTITY FULL aplicado. (3) get_manual_stock_estoque contava type=in AND billing_order_id IS NULL como entrada — ramo morto (o mirror trigger nunca insere in sem billing_order_id e save_manual_stock_manual_entry só aceita adjust_in/adjust_out), mas uma inserção manual futura de in inflaria indevidamente o KPI Entradas manuais e o Estoque físico; RPC reescrita para restringir entrada a adjust_in/adjust_out. Sem impacto em src/pages/StockMalhaManual.tsx (mesma assinatura e contrato JSON preservados). Página, triggers mirror_of_to_manual_stock/mirror_of_delete_to_manual_stock, save_manual_stock_manual_entry e get_manual_stock_movements auditados e sem outros problemas.
  - **25/07/2026 (Brasília) — Pente fino OF edit preservativo:** Auditoria da entrega `edit_billing_order` (preservação de paletes na volta para Aberto). Bug real corrigido: se o admin, na mesma edição que dispara o revert-to-open, trocasse `client_id`, `article_id` ou `machine_id`, os paletes/reservas continuariam apontando para as CHAVES ANTIGAS enquanto a OF passaria a apontar para outras — quebrando o `collect_billing_order` (que agrupa reservas por article/client/machine) e produzindo saldos incorretos em `stock_movements`/`own_stock_movements`. Correção em `public.edit_billing_order`: quando `p_revert_to_open=true`, calculamos `v_keys_changed` comparando client/article/machine do payload contra `billing_orders.*`; se qualquer chave mudou, forçamos `v_has_pallets:=false` para cair no branch antigo (release de reservas + wipe de paletes + restore de own_stock), com reason de auditoria diferenciada ('editada — chaves alteradas, reservas liberadas'). Auditoria e retorno do RPC ganham `keys_changed: boolean`. Sem mudança em `useBillingOrders.ts`/`BillingOrders.tsx` (assinatura preservada). Também revisado: o modal de edição em `BillingOrders.tsx` só permite revert a partir de `separating`/`ready` (nunca de `collected`), então o cenário collected→open via edit não é atingível pela UI. tsgo --noEmit limpo.
  - **25/07/2026 (Brasília) — OF edição preserva paletes na volta para Aberto:** Nova versão de `edit_billing_order`: quando o admin edita uma OF que estava em `separating` ou `ready` e ela volta para `open`, os paletes já lançados, as reservas em `stock_movements` e o consumo de estoque próprio ficam INTACTOS — objetivo: expedição não precisa refazer a separação. Regra: se `billing_order_pallets` existir para a OF, o revert-to-open é preservativo (não chama `_of_release_pending_reserves` nem `_of_restore_own_stock_and_wipe_pallets`), mantém `separated_by` e reidrata `pieces_real/weight_real/weight_avg` a partir da soma dos paletes (o trigger `sync_billing_order_from_pallets` só recalcula em ready/collected, então precisamos escrever manualmente aqui). Sem paletes, o comportamento antigo é mantido (libera reservas globais criadas pelo launch_ready sem paletes e devolve estoque próprio residual). `collected_by/at` e `delivery_doc_*` sempre zeram na volta para Aberto. Auditoria (`billing_order_edit`) e retorno do RPC ganham `pallets_preserved: boolean`. Sem impacto no fluxo posterior: `launch_billing_order_ready` já detecta `pallet_count > 0` e usa a soma dos paletes sem duplicar reservas; `collect_billing_order` continua netando reservas por (article,cliente,máquina). Nenhuma mudança em `useBillingOrders.ts`/`BillingOrders.tsx` (assinatura do RPC preservada).
  - **25/07/2026 (Brasília) — Pente fino OF Fases 1–4:** Auditoria completa das RPCs entregues nas Fases 1–4 de `docs/rpcBillingOrders.md`. Dois bugs reais corrigidos: (1) `edit_billing_order` ignorava silenciosamente o campo `of_number` — o modal de edição em `src/pages/BillingOrders.tsx` permite trocar o número da OF, mas o RPC nunca aceitava a chave (whitelist na hook e o UPDATE do RPC não referenciavam `of_number`), então qualquer alteração era descartada sem aviso. Reescrito o RPC para aceitar `of_number` com validação de unicidade dentro do tenant (retorna {ok:false, error:duplicate_of_number, existing_id} sem estourar 23505 e {ok:false, error:invalid_of_number} para string vazia); EXCEPTION WHEN unique_violation cobre corrida com create simultâneo; auditoria registra prev_of e of para rastrear a troca. Em `src/hooks/useBillingOrders.ts` o array `allowed` da editOrder passou a incluir `of_number` e o handler reconhece os novos códigos de erro DUPLICATE_OF/INVALID_OF. (2) `revert_billing_order_to_open` só liberava reservas — não devolvia o estoque físico quando invocado a partir de `collected` (branch previsto no plano §144). Adicionado bloco equivalente ao de `cancel_billing_order` na origem coletada: own_stock_movements do tipo in por palete com own_article_id e stock_movements do tipo in agregando SUM(out−in) líquido por (article_id, client_id, machine_id, is_second_quality) — evita saldo negativo residual caso qualquer UI/rotina futura acione a reversão direto de coletada. Nenhuma mudança de schema/RLS/GRANT. tsgo --noEmit limpo.
  - **25/07/2026 (Brasília) — OF Fase 4 (auxiliares server-side):** Implementada a Fase 4 de `docs/rpcBillingOrders.md`. 3 novas RPCs SECURITY DEFINER (search_path=public, GRANT EXECUTE anon/authenticated/service_role, validação get_user_company_id()=p_company_id senão Acesso negado/42501): (1) `get_billing_order_negative_warning(p_company_id, p_article_id, p_requested_pieces, p_requested_kg)` — replica a lógica de fetchArticleBalance no servidor agregando productions (weight_kg/rolls_produced) + stock_movements (adjust_in/adjust_out, in com/sem billing_order_id, out, reserve, release) ignorando 2ª qualidade; devolve {available_kg, available_pieces, requested_kg/pieces, after_kg/pieces, is_already_negative, will_go_negative, article_name}. (2) `get_billing_order_link_group(p_company_id, p_group_id)` — devolve {rows: [{id, of_number, status, priority, dyehouse, pieces_expected/real, weight_expected/real, delivery_doc_*, client_name, article_name, machine_name}], totals: {total_orders, total_pieces_expected/real, total_weight_expected/real, active_count, collected_count, cancelled_count}} para o modal Atrelar OFs. (3) `get_billing_order_export(p_company_id, p_id)` — payload consolidado para o PDF: {company:{id,name,logo_url,slug}, order: billing_orders.* + client/article/machine/creator/separator/collector/prioritizer/editor resolvidos como jsonb, pallets: [id, pallet_number, pieces, weight_kg, machine_name, alt_client_name, alt_article_name]}; RAISE P0002 quando fora do tenant. Em src/pages/BillingOrders.tsx a antiga fetchArticleBalance foi removida e substituída por checkNegativeWarning(articleId, reqPieces, reqKg) — wrapper fino que chama a RPC e devolve o payload; handleCreate consome os campos server-side (is_already_negative/will_go_negative + available/after) para exibir o modal de aviso sem carregar stock_movements no cliente. Nenhuma mudança em API pública/estado do componente; PDF (handleAdminPrintPdf) e modal Atrelar continuam funcionando com os dados já carregados via useBillingOrdersList — as RPCs get_billing_order_export e get_billing_order_link_group ficam disponíveis para consumo sob demanda (queries futuras podem migrar sem tocar no schema). Nada de schema/RLS/GRANT em tabelas mudou. tsgo --noEmit limpo.
  - **25/07/2026 (Brasília) — OF Fase 3 (transições de status via RPC atômicas):** Implementada a Fase 3 de `docs/rpcBillingOrders.md`. 12 novas funções `SECURITY DEFINER SET search_path=public` com `GRANT EXECUTE` a anon/authenticated/service_role e validação obrigatória `get_user_company_id() = p_company_id` (senão Acesso negado/42501): (1) `start_billing_order_separation` (open→separating, idempotente); (2) `set_billing_order_delivery_doc` (NF/Romaneio só quando status='ready' e delivery_doc_number IS NULL, devolve {conflict:{current_number}} em digitação simultânea); (3) `set_billing_order_priority` (marca/desmarca prioridade zerando motivo/autor); (4) `link_billing_orders` (merge escolhendo o MENOR link_group_id vivo entre os alvos + união dos membros); (5) `unlink_billing_order_group` (dissolve grupo); (6) `remove_from_billing_order_group` (remove 1 OF e dissolve grupo com só 1 remanescente); (7) `edit_billing_order` (COALESCE em client_id/article_id/machine_id/dyehouse/pieces_expected/weight_expected/piece_weight_target/order_type/admin_notes/priority/priority_reason + edit_note/last_edited_by/at; modo p_revert_to_open zera pieces_real/weight_real/weight_avg/separated_by/collected_by/collected_at/delivery_doc_*, libera reserva net, restaura estoque próprio e apaga paletes); (8) `create_billing_order` (gera of_number server-side sob pg_advisory_xact_lock para prevenir corrida entre admins; se vier duplicado retorna {ok:false, error:'duplicate_of_number', existing_id} sem estourar 23505); (9) `launch_billing_order_ready` (separating→ready; quando há paletes usa SUM(pieces/weight_kg) como pieces_real/weight_real; só cria reserve global quando NÃO há paletes); (10) `collect_billing_order` (ready→collected; libera reservas por chave (article,cliente,máquina) via netByKey — sem release-fantasma — e emite out líquido; fallback para OFs legadas sem reserva agrupa paletes por máquina; paletes own_stock ficam fora do stock_movements); (11) `cancel_billing_order` (cancela de qualquer status ≠ cancelled; separating/ready libera reserva pendente + apaga paletes + restaura estoque próprio; collected estorna preservando distribuição por máquina, respeita 1ª/2ª qualidade e devolve tanto em stock_movements 'in' quanto em own_stock_movements 'in' para paletes 'own'); (12) `revert_billing_order_to_open` (força volta para Aberto a partir de separating/ready/collected — libera reservas net, apaga paletes, restaura estoque próprio, zera documento/coletor/coletado_em e grava reverted_from/reversed_by/reversed_at/reversal_reason). Todas gravam evento no audit_logs com author_name/author_code. Três helpers privados (_of_release_pending_reserves, _of_restore_own_stock_and_wipe_pallets, _of_audit) centralizam a lógica reusada. Em `src/hooks/useBillingOrders.ts` toda a implementação de createOrder/updateStatus/editOrder/setDeliveryDoc/linkOrders/unlinkGroup/removeFromGroup foi reescrita para chamar exclusivamente as RPCs — a API pública (nomes, tipos, códigos de erro DUPLICATE_OF/CONFLICT, toasts, invalidações) permanece inalterada e `src/pages/BillingOrders.tsx` não precisou de mudança. updateStatus faz dispatch entre as RPCs conforme o status alvo. Conflitos {ok:false,error:'conflict'} reconstroem o objeto {code:'CONFLICT',currentStatus,actor} esperado pela página (fetch secundário só quando há colisão). Toda a lógica de estoque no cliente (restoreOwnStockForOrder, netByKey, inserts diretos em stock_movements/own_stock_movements) foi removida do hook — vive só nas RPCs. Escritas diretas em billing_order_pallets/stock_movements restantes em BillingOrders.tsx são apenas o CRUD interativo de paletes durante a separação (Fase 4). Nada de schema/RLS/GRANT em tabelas mudou.
  - **25/07/2026 (Brasília) — OF Fase 2 (leituras paginadas server-side):** Implementada a Fase 2 de `docs/rpcBillingOrders.md`. Duas novas funções SECURITY DEFINER (search_path=public, GRANT anon/authenticated/service_role, validação obrigatória get_user_company_id() = p_company_id, senão Acesso negado/42501): (1) `public.get_billing_orders_list(p_company_id, p_view, p_search, p_client_id, p_month, p_start_date, p_end_date, p_page, p_page_size)` — filtro por aba (priority|open|separating|awaiting_doc|ready|collected|cancelled|all) com regra awaiting_doc = ready AND delivery_doc_number IS NULL e ready = ready AND delivery_doc_number IS NOT NULL aplicada no servidor; busca ILIKE em of_number/dyehouse/doc/cliente/artigo; janela por mês (YYYY-MM em America/Sao_Paulo) ou range de datas aplicada a collected_at/cancelled_at conforme a aba; ordenação priority DESC, created_at DESC para abas ativas e collected_at DESC / cancelled_at DESC para Coletadas/Canceladas; paletes agregados via jsonb_agg com nome da máquina; link_group_size computado por COUNT(*) por grupo; retorno {rows, total_count, page, page_size} e clamp de page_size entre 1 e 200. (2) `public.get_billing_order_detail(p_company_id, p_id)` — payload completo para o modal do olho (campos da OF + nomes/códigos de criador, separador, coletor, cancelador, editor, priorizador, quem lançou doc, quem reverteu + paletes ordenados + link_group_size); erra OF não encontrada (P0002) quando fora do tenant. Em `src/hooks/useBillingOrders.ts` novos hooks `useBillingOrdersList(params)` (com keepPreviousData e staleTime 15s) e `useBillingOrderDetail(id)` disponíveis para a página migrar aba por aba; o canal Realtime existente passa a invalidar também [billing_orders_list, companyId] e [billing_order_detail]. O `orders` legado do hook permanece exportado sem mudança para não quebrar o consumo atual — a substituição dos useMemo de filtro/paginação em `BillingOrders.tsx` será feita na etapa seguinte. Nenhuma alteração de schema/RLS/GRANT em tabelas.
  - **25/07/2026 (Brasília) — OF Fase 1 (bootstrap RPC):** Implementada a Fase 1 de `docs/rpcBillingOrders.md`. Nova função `public.get_billing_orders_bootstrap(p_company_id)` (SECURITY DEFINER, search_path=public, GRANT anon/authenticated/service_role) devolve em uma chamada: company (id/name/logo_url/slug), stats (open, priority, separating, awaiting_doc, ready, collected_month, cancelled_month) — `awaiting_doc` = status=ready AND delivery_doc_number IS NULL; mês corrente em America/Sao_Paulo —, available_months (DISTINCT to_char(collected_at, YYYY-MM) UNION mês atual, DESC), next_of_number/last_of_number (maior numérico via regex + 1, pad-3) e link_groups_count. Validação de tenant obrigatória (get_user_company_id() = p_company_id, senão Acesso negado/42501). Em `src/hooks/useBillingOrders.ts` novo `useQuery([billing_orders_bootstrap, companyId], staleTime 60s)` exposto no retorno; canal Realtime existente passa a invalidar também a chave do bootstrap; `getNextOfNumber` foi reescrito para chamar a RPC (elimina o SELECT of_number massivo cliente-side). Em `src/pages/BillingOrders.tsx` o useEffect que abre o modal Nova OF prefere bootstrap.next_of_number quando disponível. Fases 2–4 permanecem pendentes conforme o plano.
  - **25/07/2026 (Brasília) — Firmware ESP32-S3-Touch-LCD-5 (IoT):** Entregue no chat firmware .ino adaptado da referência de docs/iot.md para a placa Waveshare ESP32-S3-Touch-LCD-5 (800x480 RGB, GT911, ESP32-S3-WROOM-1 N16R8). Diferenças vs. DevKit clássico: sensor movido para GPIO 17 (GPIOs 2/4 são do LCD nesta placa), remoção dos LEDs verde/vermelho substituídos por painel touch com status ao vivo (Wi-Fi, RPM, rotações desde o boot, contador de envios OK/falhas, último HTTP code, SSID, RSSI), toque em qualquer ponto força envio imediato + refresh, bibliotecas: Arduino_GFX (moononournation), TAMC_GT911, ArduinoJson. Contrato do POST para machine-webhook preservado (mesmos campos: company_id, machine_id, total_rotations, rpm, is_running, uptime_ms, wifi_rssi) — nenhuma mudança de backend/RLS/schema.
  - **24/07/2026 (Brasília) — Overlay de progresso de compressão + upload:** Novo `src/lib/uploadProgress.ts` (store singleton subscribe/emit) + `src/components/UploadProgressOverlay.tsx` (overlay global fixed z-9999, barra de progresso, chips Comprimindo/Enviando, auto-close em success/erro). Montado em `src/App.tsx`. Instrumentados os 5 pontos de compressão+upload: `useFreightOrders.completeOrder` e `saveFreighterEdit` (OFR), `MaintenanceOrdersTab.saveOrder` (drafts na criação de OC/OE), `MaintenanceOrdersTab.uploadPhoto` (foto avulsa em OC em curso) e o fluxo Abrir OE via OC (`escalate_oc_to_oe`). Cada foto emite compressing → uploading e no fim finalizing → done; erros disparam fail(msg). Percent estimado por foto (compress 40%, upload 90%). Sem mudança de schema/RLS.
  - **24/07/2026 (Brasília) — Pente fino overlay de progresso (guarda total=0):** Em `src/lib/uploadProgress.ts` o `start(title, total)` agora não abre o overlay quando `total <= 0` (antes clampava para 1 e mostrava "Foto 1 de 1" espúrio caso o chamador dispare sem fotos — ex.: OFR finalizada sem anexo). `step()` e `done()` viraram no-op quando o overlay não está aberto, evitando que uma sequência de chamadas subsequente reabra a UI vazia. Nenhum caller precisou mudar; comportamento inalterado quando há ao menos 1 foto.
  - **24/07/2026 (Brasília) — Mecânica Movimentações paginação 20/pág:** Em `src/components/mecanica/MaintenanceMovementsTab.tsx` as sub-abas "Movimentações Gerais" e "Por Máquina" ganharam paginação numérica de 20 em 20 (states `geralPage`/`maqPage`). Rodapé mostra "Mostrando X–Y de Z" + botões "Anterior / 1 2 3 … N / Próxima" (janela ±1 com reticências). Troca de máquina reseta `maqPage`; alteração do feed reseta `geralPage`. Sem impacto em RPC/banco.
  - **24/07/2026 (Brasília) — Pente fino OFR "Outros":** Em `src/pages/FreightOrders.tsx` corrigidos três vazamentos deixados pela introdução do tipo `outros`: (1) badge `CONTÉM MALHA` no `OrderCard` usava `item_type !== "fio"` e marcava OFRs que só tinham itens `outros` como "malha" — agora `hasMalha` exige `!item_type || === "malha"` e foi adicionado novo badge âmbar `CONTÉM OUTROS`; (2) `ItemsBreakdown` (usado no modal Detalhes) misturava itens `outros` dentro da seção Malhas (inclusive somando peças/kg incorretamente) — agora `outros` tem seção própria em âmbar com descrição, peças/cx/kg opcionais, e o `kgTotal` do cabeçalho passa a somar Malha + Fio + Outros; (3) o filtro de busca da página não olhava para o campo `description` — passou a incluir `description` do item além de artigo/fio. Sem migração — apenas ajustes de renderização/filtro.
  - **24/07/2026 (Brasília) — OFR item tipo "Outros":** Em `src/pages/FreightOrders.tsx` (modal Nova/Editar OFR) o seletor de tipo de item ganhou opção "Outros" além de Malha/Fio. Ao escolher Outros abre um campo obrigatório de descrição livre + peças/caixas/peso opcionais. Migração ampliou o CHECK de `freight_order_items.item_type` para aceitar `'outros'` e adicionou coluna `description text`. Hook `useFreightOrders` (createOrder/updateOrder) persiste `description`. Card da OFR mostra ponto âmbar + descrição como nome. PDF (`src/lib/freightOrderPdf.ts`) ganha seção "Outros" com colunas Descrição/Peças/Cx/Kg. Filtros existentes tratam `outros` como não-fio (fica fora dos badges CONTÉM FIO/MALHA).
  - **24/07/2026 (Brasília) — Pente fino Ordens (mobile + modal galeria):** Em `src/pages/Ordens.tsx` o container raiz passou a usar `-mx-4 md:mx-0` para anular o `p-4` do `main` no mobile e ganhar largura total (cards deixaram de ficar espremidos). `CardContent` reduzido para `px-2 sm:px-6` e cada card com `p-3 sm:p-4`, `gap-3 sm:gap-4` no grid. O `DialogContent` da galeria de fotos agora respeita o padrão do projeto: 100vw/100vh no mobile e 80vw/80vh em sm+. Nenhum impacto em lógica/RLS/banco.
  - **24/07/2026 (Brasília) — Nova aba "Ordens" (Máquinas Paradas dedicada):** Bloco "Máquinas Paradas" removido de `src/pages/Dashboard.tsx` e migrado para nova página `src/pages/Ordens.tsx` (rota `/:slug/ordens`, `routeKey=ordens`). Lista em tempo real (Realtime) todas as ordens em execução — `maintenance_orders.status=em_curso` (cobre OM/OC/OE) e OTs em `troca_fio_em_curso`/`aguardando_regulagem`/`em_regulagem`/`em_acompanhamento` — com cards enriquecidos: número/tipo/prioridade, máquina + modelo/Ø/finura, descrição, autor da criação e da etapa, timer HH:MM:SS ao vivo, últimas 3 anotações (`progress_notes`) e miniaturas de `oc_photos` (signed URLs 1h) com modal de galeria ampliada. Em `src/components/AppSidebar.tsx` novo item "Ordens" (ícone `PauseCircle`) inserido ACIMA de "Dashboard" e só aparece quando há ordens em execução (soma `maintenance_orders em_curso` + estágios ativos de OT, ambos com contagem realtime); badge âmbar + pulse verde exibe a contagem. `enabled_nav_items` é ignorado para a chave `ordens` (mesmo tratamento das sub-abas de Mecânica). Em `src/hooks/usePermissions.ts` a chave `ordens` foi adicionada a `admin`, `lider`, `lider_noite`, `lider_mecanica`, `mecanico` e `eletricista`, e mapeada em `ROUTE_KEY_MAP`. Nenhuma mudança de schema.
  - **24/07/2026 (Brasília) — Pente fino Mecânica OE (labels, push e logs):** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` corrigidos vazamentos onde OEs eram tratadas como OMs pelo código legado: (1) `startOrder` — mensagem de permissão agora reconhece OE ("Apenas admin ou eletricista podem iniciar OEs"), toast/log de início usam label `OE` e chave `oe_start` com `{ oe: oe_number }`; (2) `confirmFinish` — toast/label/log usam `finLabel`, `oe_finish` grava `{ oe: oe_number, duration_s, items }`, push de finalização vira `OE #NNN finalizada — Máquina`, roles alvo trocam para `['eletricista']` (mantendo `include_admins`), `source` = `OE`, `ref_number` correto e `url` aponta para `/{slug}/mecanica/oe` em vez de `/mecanica/om`; (3) `cancelOrder` — label `OE`, chave `oe_cancel` com `{ oe: oe_number }`. Sem esses ajustes, a finalização/cancelamento de uma OE gerava push "OM #NNN finalizada", registrava `om: null` no audit (OE não tem `om_number`) e mandava o clique da notificação para `/mecanica/om`. Nenhuma mudança de schema — `finalize_maintenance_order` já reseta `machines.status = 'ativa'` para qualquer type.
  - **24/07/2026 (Brasília) — Eletricista sem LoadingScreen:** Em `src/components/AppLayout.tsx` o papel `eletricista` foi incluído na lista de exceções (`isEletricista`) que ignora o `LoadingScreen` de "carregando dados da empresa", junto com expedição, líder, mecânico, líder mecânica, líder noite, freteiro e líder frete. Login do eletricista agora entra direto na UI sem esperar o pré-carregamento pesado do `CompanyDataContext`.
  - **24/07/2026 (Brasília) — Configurações Usuários paginação 15/pág:** Em `src/pages/Settings.tsx` a lista de usuários da aba "Usuários" agora pagina 15 em 15 (novo state `usersPage`). Rodapé mostra "Mostrando X–Y de Z" + botões "Anterior / 1 2 3 … N / Próxima" (janela ±1 em torno da página atual com reticências). Alterar a busca reseta para página 1. Sem impacto em dados/RLS.
  - **24/07/2026 (Brasília) — Pente fino compressão de fotos (OC + OFR):** Em `src/hooks/useFreightOrders.ts` (`completeOrder` e `saveFreighterEdit`) e `src/components/mecanica/MaintenanceOrdersTab.tsx` (`saveOrder`), o `import("@/lib/imageCompression")` foi hoistado para fora do `for` (carrega uma única vez por operação em vez de por foto). A derivação da extensão do arquivo passou a sanitizar caracteres não-alfanuméricos também nos dois pontos de OFR — evita path inválido no bucket `freight-photos` quando `File.name` original vem com espaços/acentos/query. Sem mudança de comportamento observável.
  - **24/07/2026 (Brasília) — Compressão de fotos no upload (OC + OFR):** Novo utilitário `src/lib/imageCompression.ts` (canvas → JPEG, máx 1600px, qualidade 0.72, pula arquivos <300 KB) aplicado nos três pontos de upload de fotos tiradas pelo celular: criação de OC (`src/components/mecanica/MaintenanceOrdersTab.tsx` → `saveOrder` drafts), anexo de foto em OC "Em Curso" (`uploadPhoto`) e finalização + edição autorizada de OFR (`src/hooks/useFreightOrders.ts` → `completeOrder` e `saveFreighterEdit`). Fallback silencioso ao original se der erro, `imageOrientation:from-image` evita rotação errada. Import dinâmico para não pesar bundle inicial. Impacto típico: fotos de 3–8 MB caem para 300–700 KB (redução de ~80–90%), com custo de 250–800 ms de CPU no celular.
  - **23/07/2026 (Brasília) — Pente fino OFR (edição autorizada — snapshot só quando preço muda):** Em `src/hooks/useFreightOrders.ts` a mutação `saveFreighterEdit` só grava `previous_price_per_kg` / `previous_total` quando o novo valor por kg é diferente do atual — se a edição só troca foto, os campos ficam `NULL` (limpando snapshot antigo). Em `src/pages/FreightOrders.tsx` o `EditHistoryBlock` passa a ocultar o "antes → agora" quando o preço não mudou, evitando exibir `R$ 10,00 → R$ 10,00` com strike-through desnecessário. Nenhuma alteração de schema.
  - **24/07/2026 (Brasília) — OC Relatórios (olho no Detalhamento):** Em `src/components/mecanica/OCReportsTab.tsx` cada linha da tabela desktop e cada card mobile do "Detalhamento" ganharam um botão `Eye` (ghost, h-7 w-7) que abre um `Dialog` (max-w-2xl, scroll interno) com o relatório completo da OC selecionada — Nº, status (badge colorida), máquina, descrição, criada/iniciada/finalizada (data+hora+autor de cada etapa) e tempo total de parada em grid `label/value`. Quando a OC tem `oc_photos`, o modal busca signed URLs (`storage.oc-photos.createSignedUrl`, 1h) e renderiza grid 1/2 col com previews clicáveis (abrem em nova aba) e legenda por foto. Cache local `detailPhotoUrls` evita re-fetch e é limpo ao fechar o modal.
  - **24/07/2026 (Brasília) — Pente fino OC (fotos órfãs ao excluir):** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` a função `deleteOrder` passou a remover, antes do `DELETE` na tabela `maintenance_orders`, todos os arquivos do bucket `oc-photos` referenciados em `oc_photos` da ordem (best effort com `.catch`, sem bloquear o fluxo em caso de 404 no storage). Corrige vazamento de fotos órfãs ao excluir OCs canceladas/finalizadas que tinham fotos anexadas seja na criação, seja em Em Curso via notas.
  - **24/07/2026 (Brasília) — OC Nova: tirar foto OU galeria:** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` a seção "Fotos do problema (opcional)" do modal "Nova OC" passou a oferecer dois botões lado a lado — "Tirar foto" (`capture="environment"` abre câmera direto no mobile) e "Escolher da galeria" (`<input type=file accept=image/*>` sem `capture`, abre o seletor de arquivos/galeria). Limite de 2 fotos mantido; ambos alimentam o mesmo `createPhotoDrafts`.
  - **24/07/2026 (Brasília) — OC Relatórios (nova aba):** Nova aba "Relatórios" em Mecânica > OC, após "Canceladas" (só aparece em `isOC`). Componente `src/components/mecanica/OCReportsTab.tsx` recebe `modeOrders` (todas OCs, todos status) + `machines`. Traz: 4 KPIs executivos (Total, Finalizadas/abertas/em curso, Tempo Total de Parada com média e máximo, Máquina mais afetada), painel de filtros (atalhos Mês atual/30d/90d/Ano/Tudo + De/Até, máquina via SearchableSelect, status, usuário — extraído de created/started/finished_by_name, parada mín/máx em minutos e busca livre por Nº/máquina/descrição/usuário), 4 gráficos recharts com tokens semânticos (Barras OCs por máquina Top10 com tempo parado em horas, Área de evolução no período — auto-agrupa por mês se >90 dias, Barras horizontais Top 8 usuários criadores com paleta rotativa, Pizza de distribuição por status), tabela de detalhamento (desktop) + cards (mobile) limitada a 200/100 registros com aviso, e exportação em CSV (BOM UTF-8, separador `;`) e PDF landscape (jsPDF+autotable com header dark, KPIs de resumo e tabela zebrada). Sem alterações no banco.
  - **24/07/2026 (Brasília) — OC com fotos já na criação:** Em `src/components/mecanica/MaintenanceOrdersTab.tsx` o modal "Nova OC — Ordem de Corretiva" ganhou seção opcional "Fotos do problema" (até 2 imagens, com preview, remoção e descrição por foto — botão com `capture="environment"` para abrir a câmera direto no mobile). Ao salvar, `saveOrder` faz o insert normal e, se houver drafts, envia os arquivos para o bucket `oc-photos` em `{companyId}/{orderId}/{uuid}.ext` e persiste o array `oc_photos` (`JSONB`) em `maintenance_orders`; se algum upload falhar, os arquivos já enviados são removidos e um toast avisa. No card da aba "Aberto" (e em Em Curso), qualquer OC com fotos passa a exibir badge roxo "N foto(s) anexada(s)" + botão "Ver fotos" que reaproveita o modal de relatório (`viewOrder`) — o modal já renderiza a seção "Fotos do problema" com URLs assinadas em qualquer status. Drafts limpos ao abrir/editar/fechar o modal (revoga `URL.createObjectURL`).
  - **24/07/2026 (Brasília) — OFR Finalizar Entrega com modo de cobrança:** Em `src/pages/FreightOrders.tsx` o modal `CompleteModal` ganhou seletor "Valor por kg" / "Valor fixo" (padrão sempre "Valor por kg" ao abrir). No modo fixo o usuário digita o total do frete e o sistema calcula `freight_price_per_kg = total / totalKg` (arredondado a 4 casas) — mantendo o wire format e a consistência de `freight_total` na tabela `freight_orders`. Preview mostra "Valor por kg (calc.)" no modo fixo e "Total do frete" no modo por kg. Nenhuma mudança de schema/hook.
  - **24/07/2026 (Brasília) — Pente fino OFR Finalizar (valor fixo preservado + validações):** Em `src/hooks/useFreightOrders.ts` a mutação `completeOrder` passou a aceitar `freight_total` opcional e, quando presente (modo "Valor fixo" do modal), grava esse valor EXATO em `freight_orders.freight_total` em vez de recalcular via `totalKg × pricePerKg` — evitando drift de centavos causado pelo arredondamento de 4 casas em `price_per_kg` derivado. Em `src/pages/FreightOrders.tsx` o `CompleteModal` agora bloqueia o submit quando o campo do modo ativo está em branco/0 (mensagens dedicadas para cada modo) e também quando `totalKg <= 0` no modo fixo (evita divisão por zero silenciosa). tsgo limpo.
  - **23/07/2026 (Brasília) — OFR botão "Aut. edição":** Em `src/pages/FreightOrders.tsx` o texto do botão "Autorizar edição" (card de OFR finalizada) foi encurtado para "Aut. edição" para não vazar da largura no mobile. Nenhuma mudança de comportamento.
  - **23/07/2026 (Brasília) — KPIs Estoque Próprio acima das abas:** Em `src/pages/StockMalha.tsx` os cards de KPI (Entradas / Saídas / Saldo) da aba "Estoque ({empresa})" foram movidos para fora do `TabsContent value="propria"` e agora renderizam no mesmo bloco condicional dos demais (`estoque` e `segunda`) — ficando acima da `TabsList` como padrão consistente entre as quatro abas. Nenhuma mudança de lógica/dados.
  - **23/07/2026 (Brasília) — OFR Edição Autorizada de Finalizados:** Novo fluxo em `src/pages/FreightOrders.tsx` + `src/hooks/useFreightOrders.ts`. Admin/líder abrem "Autorizar edição" em uma OFR finalizada (motivo opcional) — a OFR ganha badge âmbar "EDIÇÃO LIBERADA" e passa a aparecer também em nova aba **Editar** (visível para admin e para o freteiro dono via RLS por `freighter_id → user_id`). O freteiro (ou admin) abre modal full-screen "Editar OFR" onde altera **somente** valor por kg e fotos (mantém, remove ou adiciona; 1–2 fotos obrigatórias). Ao salvar, `saveFreighterEdit` snapshota `previous_price_per_kg` / `previous_total`, recalcula `freight_total = totalKg × novo preço`, move fotos removidas para `freight_order_edit_photos` (histórico permanente, sem apagar do bucket), sobe novas em `freight_order_photos` e limpa flags de autorização. A OFR volta a Finalizados com badge "EDITADA" e, em Detalhes, seção "Histórico de Edição" com valores antes → agora (strike-through) e miniaturas das fotos removidas. Push: freteiro notificado ao autorizar, admins ao concluir. Realtime cobre `freight_order_edit_photos`. Admin pode revogar a autorização a qualquer momento.
  - **23/07/2026 (Brasília) — Estoque Próprio: artigos vindos de Clientes & Artigos:** Em `src/components/OwnStockManualModal.tsx` o campo "Artigo *" perdeu a opção "Criar novo" e agora lista exclusivamente artigos cadastrados em Clientes & Artigos (tabela `articles`), formatados como "Artigo (Cliente)" seguindo o padrão do sistema. RPC `save_own_stock_movement` estendida para aceitar `article_name` — quando recebida, localiza (case-insensitive) ou cria automaticamente o item correspondente em `own_stock_articles`, mantendo o FK `own_article_id` intacto e o histórico consistente. Compatibilidade preservada com o payload antigo (`own_article_id` segue funcionando).
  - **23/07/2026 (Brasília) — Pente fino OFR (fotos órfãs / cancelamento defensivo):** Em `src/hooks/useFreightOrders.ts` a mutação `completeOrder` passou a: (1) pré-checar o status antes do upload das fotos, abortando com mensagem clara quando a OFR já não está mais em curso; (2) validar o retorno do UPDATE final com `.select().maybeSingle()` e, se 0 linhas afetadas (status alterado no meio), remover os arquivos recém enviados do bucket `freight-photos` via `storage.remove`; (3) só inserir as linhas em `freight_order_photos` DEPOIS do UPDATE bem sucedido — elimina fotos órfãs no storage/DB em corridas com edição/cancelamento. `cancelOrder` agora restringe o UPDATE via `.in('status', ['open','pickup_in_progress','delivery_in_progress'])` e valida retorno, evitando "cancelar" OFRs já finalizadas/canceladas. Nenhuma mudança de UI.
  - **23/07/2026 (Brasília) — Botões OFR mobile-first (Iniciar/Finalizar full-width):** Em `src/pages/FreightOrders.tsx` a linha de ações do `OrderCard` voltou ao padrão mobile: no mobile o botão principal ("Iniciar Frete" ou "Finalizar Entrega") ocupa 100% da largura com h-12 e ícone maior; os secundários (Detalhes, Endereços, PDF, Editar, Prioridade, Cancelar) ficam em grid 2 colunas com Cancelar span-2. No sm+ tudo volta a ser flex-wrap horizontal com o principal auto-width e Cancelar empurrado à direita via ml-auto. Usa sm:contents para o wrapper do grid virar inline no desktop sem duplicar markup. Nenhuma mudança de comportamento. tsgo limpo.
  - **26/07/2026 (Brasília) — Estoque Malha (Manual):** Novo módulo paralelo em `src/pages/StockMalhaManual.tsx` (rota `/:slug/estoque-malha-manual`, item de sidebar abaixo de "Estoque Malha"). Backend com tabela `manual_stock_movements` (multi-tenant RLS via `get_user_company_id()`) e triggers `mirror_of_to_manual_stock` / `mirror_of_delete_to_manual_stock` que **espelham automaticamente da `stock_movements` apenas os eventos ligados a OF** (types `reserve`/`release`/`out` e `in` com `billing_order_id`), preservando `source_movement_id` para estorno consistente. Produção NÃO alimenta o armazém manual — todas as entradas são feitas pelo usuário. Backfill executado no momento da migração. RPCs `get_manual_stock_bootstrap`, `get_manual_stock_estoque`, `get_manual_stock_movements`, `save_manual_stock_manual_entry` (valida motivo mínimo 5 chars, resolve `profiles.id` correto via `auth.uid()`+`company_id`). Página com abas Estoque + Movimentações, KPIs, filtros (cliente/artigo/mês, tipo/data/OF), cards colapsáveis por máquina, paginação server-side de 20 em 20, realtime via `postgres_changes` na tabela. `REPLICA IDENTITY FULL` + publicação `supabase_realtime` incluídas. Permissão: admin e expedição.
  - **27/07/2026 (Brasília) — Estoque Malha (Manual) grupos fechados por padrão:** Em `src/pages/StockMalhaManual.tsx` os `Collapsible` de clientes na aba Estoque perderam o `defaultOpen` — agora iniciam fechados, exigindo clique no header para expandir. Também adicionadas guardas `|| []` em `g.articles` e `a.byMachine` para eliminar `TypeError: Cannot read properties of undefined (reading map)` quando a RPC devolvia grupo sem articles/byMachine.
  - **27/07/2026 (Brasília) — Estoque Malha (Manual) totais por cliente:** RPC `public.get_manual_stock_estoque` passou a devolver `totalEntradaKg / totalDeliveredKg / totalStockKg / totalStockRolls / totalReservedKg / totalAvailableKg` por cliente (agregando o mesmo `per_article` já usado nos KPIs). Antes o header do Collapsible de cliente em `src/pages/StockMalhaManual.tsx` lia esses campos, que não existiam no payload, e exibia 0,00 kg em Entradas/Reservado/Disponível mesmo com os KPIs somando entregas e reservas. Nenhuma mudança de schema/RLS/GRANT.
  - **27/07/2026 (Brasília) — Pente fino Push de teste ("Somente eu"):** A edge function `send-push-notification` sempre removia `uid` (autor) da lista de destinatários, o que fazia o card de teste em Configurações → Empresa disparar com `target_user_ids=[uid]` e retornar `no_targets` (0 enviados) quando o admin escolhia "Somente eu". Adicionado flag opcional `allow_self: true` no body: quando presente, a função pula a linha `userIdsSet.delete(uid)` e o autor recebe o push. `TestPushNotificationCard` passa `allow_self=true` apenas no modo self; demais audiences continuam com o comportamento anterior (autor nunca se notifica). Edge function redeployada.
  - **02/08/2026 (Brasília) — OF auditoria detalhada de separação e paletes:** Novas colunas em `billing_orders` (`separation_started_by/at`, `separation_finished_by/at`) preenchidas pelas RPCs `start_billing_order_separation` (início) e `launch_billing_order_ready` (envio para Aguardando NF/ROM); backfill de registros antigos com `separated_by`. `get_billing_orders_list`, `get_billing_order_detail` e `get_billing_order_export` passaram a expor nomes/códigos de quem iniciou e finalizou a separação, além de `created_by_name/code` e `created_at` de cada palete. Em `src/pages/BillingOrders.tsx`: cards mostram "Sepa. inic." e "Sepa. fina." com data/hora e mantêm "Coletado"; o modal Detalhes exibe média kg/pç palete a palete (mais média total) e coluna de auditoria por palete oculta no mobile (`hidden sm:table-cell`); o PDF interno recebeu seção "PALETES (N) - AUDITORIA" com peças, peso, média, máquina, autor e data/hora de cada palete, além de linhas de separação iniciada/finalizada e coleta na seção AUDITORIA.
