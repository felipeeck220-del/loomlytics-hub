  - **19/07/2026 (Brasília) — docs/fechamentomensal.md atualizado:** Reescrito 100% aderente ao código atual de src/pages/Fechamento.tsx (~1.005 linhas). Documenta propósito (consolidar em um único relatório: balanço de fio, produção, estoque de malha, receitas próprias, receitas/prejuízos terceiros, resíduos, venda de fio, estoque em facções, faturamento total), permissões (admin + enabled_nav_items.fechamento), 10 tabelas consumidas somente leitura (invoices sem status cancelada, invoice_items, productions, articles, yarn_types, outsource_productions com snapshots, outsource_companies fallback, outsource_yarn_stock filtrado por reference_month, residue_sales, companies só no PDF), arquitetura (fetchAll paginação 1000, isInMonth/isUpToMonth lexicográficos yyyy-MM-dd, isSulBrasil normalizado, prevMonth via date-fns, 9 Promise.all, 4 maps + 3 helpers useCallback, 10 useMemo section1..section10), fluxo de carregamento sob demanda (loaded reseta ao trocar mês), fórmulas exatas de cada seção (KG estoque=compra-consumo-vendas até o mês, saldo por yarn_type omitindo tipos inertes, estoque de malha somente Sul Brasil hard-coded, receitas próprias agrupadas por cliente+artigo com valuePerKg=revenue/kg, terceiros bifurcados por sinal de total_profit do próprio lançamento podendo aparecer em ambas seções, resíduos por cliente+material, venda de fio 1 linha por item, estoque em terceiros por facção, faturamento total = próprias.revenue + terceiros.profit + prejuízos.profit(negativo) + resíduos.total + vendaFio.total com assimetria intencional), UI (SectionCard, tabelas com TOTAL bold, Collapsible cliente/facção 3 fechado 9 aberto, empty states, cores text-destructive/text-success/font-mono), PDF (jsPDF+autoTable A4 retrato, addHeader 25mm com logo canvas crossOrigin+fitWithinBox 24x14, 10 páginas fixas mesmo vazias, sanitizePdfText, arquivo Fechamento_yyyy-MM_Nome.pdf), 12 riscos (volume no cliente sem filtro server-side, sem RPC, lexicográfico depende de text, hard-coded Sul Brasil, cancelamento só em invoices dependendo do guard if(!inv), reference_month protegido por setLoaded(false), profit assumido negativo, valuePerKg=0 mascara preço, PDF páginas quase em branco, sem audit_log de exportação, deps silenciosas de invoiceMap, companies fetch por export), integrações (Faturamento Total usa RPC get_faturamento_total_metrics enquanto Fechamento recalcula tudo, StockMalha compartilha lógica sem restrição Sul Brasil, Saldo Global de Invoices replica Seção 2, RLS get_user_company_id em todas as 9 tabelas) e checklist 10 pontos. Ganchos futuros listados (get_fechamento_bootstrap/export_payload, parametrizar Sul Brasil, audit_logs fechamento_export, fechamento_snapshots) — nada implementado, apenas documentação.
  - **19/07/2026 (Brasília) — docs/rpcresiduesales.md criado:** Plano de migração em 4 fases para src/pages/ResidueSales.tsx seguindo padrão consolidado (rpcInvoices/rpcclientInvoices/rpcstockMalha/rpcBillingOrders/rpcFreightOrders/rpcproduction/rpcrevision/get_faturamento_total_metrics). Regras imutáveis: comportamento visível preservado 100% (3 abas, filtros mês+De/Até+busca em série, paginação 20/pg janela 5, KPIs, multi-lançamento, PDF landscape A4), schema intocado (date continua text yyyy-MM-dd, material_id continua CASCADE, default_price legado forçado 0, UNIQUE (client_id,material_id) preservado). Fase 1 get_residue_sales_bootstrap (company+logo + materials + clients + client_prices + available_months distinct yyyy-MM DESC + global_stats total/valor/kg/un, elimina 4 useQuery + useEffect de logo). Fase 2 get_residue_sales_list paginada server-side (p_from/p_to/p_month/p_client_id/p_material_id/p_search com unaccent+ILIKE em material_name/client_name/romaneio, p_page/p_page_size cap 200, ordenação created_at DESC + id ASC estável, KPIs filtrados no mesmo payload compartilhando CTE, contrato {rows,total_count,page,page_size,kpis}). Fase 3 escritas atômicas: save_residue_material/delete (default_price forçado 0, delete devolve cascade_sales_deleted); save_residue_client/delete (delete devolve cascade_prices_deleted e sales_disassociated por SET NULL); save_residue_client_price/delete (intercepta UNIQUE devolvendo {conflict:duplicate_client_material,existing_id} sem 23505); save_residue_sale(p_id?,p_payload,p_author_name,p_author_code) atômica com validações espelho (client_id+material_id da mesma company, date ±5 anos regex, quantity>0, unit_price>0, snapshots material_name/client_name/unit re-lidos server-side jamais aceitos do cliente, total=round(qty*price*100)/100, autoria só no INSERT preservada no UPDATE, idempotência opcional por romaneio+client+material+date, audit_logs residue_sale_create/update embutido); delete_residue_sale idempotente com FOR UPDATE + audit residue_sale_delete + {ok,already}. Fase 4 get_residue_sales_export_payload (rows+totals+period_label+company logo com date_br server-side, permite PDF cobrir base completa após Fase 2). Todas SECURITY DEFINER search_path=public com v_caller := get_user_company_id() + RAISE Acesso negado em escritas, GRANT anon/authenticated/service_role, contrato {ok,already,id,action,conflict} idêntico às demais RPCs. Documento inclui refactor cliente por fase (useQuery keepPreviousData, invalidação bootstrap+list+faturamento_total_metrics, useDeferredValue opcional em searchText, remoção de filtered/paginated/kpis memos, remoção de logAction após deploy, skipPriceAutoUpdate ref mantida como UX pura), tabela de invalidação por mutation, 12 riscos críticos (destaque para material_id CASCADE apagar vendas explicitado via cascade_sales_deleted, UNIQUE amigável, snapshots vs catálogo, total server-side, auditoria dupla transitória, RLS ativa), checklist em 22 pontos e ordem sugerida (Fase 1 → Fase 2 → Fase 4 → Fase 3.7-3.8 → Fase 3.1-3.6 → cleanup). Fora do escopo: migrar date text→date, material_id CASCADE→SET NULL, remover default_price legado, índice (company_id,date DESC), realtime, RPC de análise por cliente. Nada implementado — apenas planejamento.
  - **19/07/2026 (Brasília) — docs/residuesales.md criado:** Documentação técnica 100% completa de src/pages/ResidueSales.tsx (~1.289 linhas) — propósito (vendas cliente-cêntricas: cliente define materiais e preços; snapshots preservam histórico), permissões admin, modelo (residue_materials com default_price legado, residue_clients, residue_client_prices UNIQUE (client_id,material_id), residue_sales com client_id SET NULL e material_id CASCADE + snapshots material_name/client_name + autoria created_by_name/code), estrutura (4 useQuery + CRUD Material/Cliente/Preço/Venda + filtros/paginação + KPIs + PDF), fluxos (mutations invalidam chaves relacionadas; deleteClient/deleteMat invalidam client_prices por cascade; formulário usa skipPriceAutoUpdate ref + duplo setTimeout 50/100 ms para vencer corrida com dois useEffect que zeram material ao trocar cliente e auto-preenchem preço; parseBR aceita 1.234,56; INSERT mantém dialog aberto para multi-lançamento preservando cliente/data; UPDATE fecha), filtros (mês + De/Até + busca em material/cliente/romaneio aplicados em série; date text yyyy-MM-dd lexicográfico), paginação 20/pg client-side janela 5, KPIs (totalValue, totalQtyKg, totalQtyUn, count), UI (3 abas cards no mobile + tabelas no desktop, 6 Dialogs, DeleteConfirmDialog global), PDF (jsPDF landscape A4 + autoTable, cabeçalho 25mm/logo canvas crossOrigin+fitWithinBox 24x14, período dinâmico, 7 col + total, sanitizePdfText), 12 riscos (destaque material_id CASCADE apaga vendas históricas divergindo de client_id SET NULL, sem paginação server-side, date text, skipPriceAutoUpdate frágil, default_price legado sempre 0, ordenação created_at DESC e não date DESC, total no cliente), integração FaturamentoTotal (get_faturamento_total_metrics) e useAuditLog, checklist 12 pontos e ganchos futuros (get_residue_sales_bootstrap/list/export_payload, save_residue_sale atômica {ok,already,conflict}, save_residue_client_price UNIQUE server-side, migrar material_id CASCADE→SET NULL, date text→date). Nada implementado — apenas documentação.
  - **19/07/2026 (Brasília) — docs/rpcrevision.md criado:** Plano de migração em 4 fases para src/pages/Revision.tsx + src/lib/queries/defectsQueries.ts + escritas de defeitos em src/hooks/useCompanyData.ts, seguindo padrão consolidado (rpcInvoices/rpcclientInvoices/rpcstockMalha/rpcBillingOrders/rpcFreightOrders/rpcproduction/get_faturamento_total_metrics). Regras imutáveis: comportamento visível preservado 100% (filtros, ordenação, colunas, PDF, modal, multi-lançamento), schema defect_records intocado (defect_name continua embutido em observations como "[Nome] obs"). Fase 1 get_revision_bootstrap (company logo_url/name + available_months distinct yyyy-MM DESC + stats globais total/total_kg/total_metros, elimina useEffect de logo e memo availableMonths, libera Revision.tsx do array global defectRecords). Fase 2 get_revision_defects_list paginada server-side (p_from/p_to/p_month/p_article_id/p_search com ILIKE em machine/article/weaver, p_page/p_page_size cap 200, ordenação date DESC + created_at DESC + id ASC estável, KPIs filtrados no mesmo payload, contrato {rows,total_count,page,page_size,kpis}); Fase 2b get_revision_defect_detail para deep-links opcionais. Fase 3 save_defect_record(p_id?,p_payload,p_author,p_code) atômica com validações espelho do handleSave (date ±5 anos regex yyyy-MM-dd, shift IN(manha,tarde,noite) com lower+unaccent, machine/article/weaver obrigatórios e verificados por company_id, measure_type IN(kg,metro), measure_value>0, defect_name obrigatório), snapshots server-side re-lidos do banco (machine_name/article_name/weaver_name jamais aceitos do cliente), serialização [defect_name] obs feita no servidor, autoria server-side apenas no INSERT preservada no UPDATE, audit_logs defect_create/update inserido pela própria RPC, contrato {ok,already,id,action} idêntico a save_client_invoice; delete_defect_record idempotente com FOR UPDATE + audit defect_delete + {ok,already}. Fase 4 get_revision_export_payload (rows + totals + period_label + company logo, extrai defect_name via regex ^\[(.+?)\] no servidor, permite PDF cobrir toda a base filtrada sem depender do array global). Todas SECURITY DEFINER search_path=public com v_caller := get_user_company_id() + early-return de payload vazio em leituras / RAISE Acesso negado em escritas, GRANT anon/authenticated/service_role. Documento inclui refactor cliente por fase (useQuery com keepPreviousData, invalidação de revision_defects_list + revision_bootstrap, useDeferredValue opcional em searchTerm, remoção de filtered/paginatedData/stats memos, remoção de logAction após deploy, addDefectRecords/updateDefectRecords/deleteDefectRecords mantidos como wrappers por retro-compat), 12 riscos críticos (multi-tenant, defect_name embutido, snapshots server-side, shift normalizado, idempotência, page_size cap, ordenação estável, auditoria dupla transitória, retro-compat, invalidação de bootstrap, RLS ativo, get_defect_stats mantido) e checklist em 22 pontos. Ordem sugerida: Fase 1 → Fase 2 → Fase 4 → Fase 3 → Fase 2b → Realtime opcional. Fora do escopo: refatorar defect_name para coluna própria, migrar date text→date, realtime obrigatório, tocar em Weavers/Reports/defectsQueries. Nada implementado — apenas planejamento.
  - **19/07/2026 (Brasília) — docs/revisao.md removido:** Arquivo antigo/duplicado excluído; documentação oficial do módulo Revisão permanece em docs/revision.md (criado em 19/07/2026) e docs/data/06-revisao.md (schema).
  - **19/07/2026 (Brasília) — docs/revision.md criado:** Documentação técnica 100% completa de src/pages/Revision.tsx (~935 linhas) + src/lib/queries/defectsQueries.ts — propósito (registrar/editar/excluir falhas de qualidade amarrando data+turno, máquina, artigo+nome do defeito, tecelão, kg/metros, autoria Nome #Código), permissões (admin + revisador com defaultRoute; controle por enabled_nav_items), modelo (defect_records 16 col com RLS get_user_company_id em 4 políticas; RPC auxiliar get_defect_stats disponível para outros consumidores), camada cliente (useSharedCompanyData carrega array global defectRecords + fetchDefectsPage paginada em defectsQueries.ts usada por Weavers/Reports; normalizeShift sem acento; mapDefectRecord defaults), regras (turnos manha/tarde/noite; defect_name serializado em observations como "[Nome] obs" com regex ^\[(.+?)\]\s*(.*); snapshots machine_name/article_name/weaver_name; autoria created_by_name/code do useAuditLog só no INSERT; multi-lançamento reseta apenas defect_name/measure_value/observations mantendo data/turno/máquina/tecelão/artigo/tipo; exclusão exige digitar EXCLUIR; audit_logs defect_create/update/delete), estado (11 estados incluindo intervalo De/Até mutuamente exclusivo com filterMonth, filterArticle com busca inline, pageSize=20, refs de auto-focus), 10 memos (sortedMachines, filtered* modais + filter, availableMonths distinct yyyy-MM DESC, filtered aplicando 5 filtros, paginatedData slice 20, stats total/totalKg/totalMetros, companyShiftLabels), UI (3 KPI cards Total/Kg/Metros; barra flex-wrap com Exportar PDF desabilitado se vazio; mobile cards + desktop table 10 col; paginação janela deslizante 5 páginas; modal 95vw/max-w-2xl ESC+clique-fora bloqueados com 4 linhas de campos e selects buscáveis avoidCollisions=false; modal delete sm:max-w-sm), PDF (jsPDF puro sem autoTable; cabeçalho 25mm com logo canvas crossOrigin+fitWithinBox 24x14 e fallback companyName; período dd/MM/yyyy até dd/MM/yyyy ou MMM/yyyy ou Total; resumo; tabela zebra 5 col Data/Máquina/Artigo/Valor/Defeito com truncamento; linha TOTAL kg+m; sanitizePdfText; arquivo revisao_yyyy-MM-dd_HHmm.pdf), integrações (Weavers ranking Verde≤3/Amarelo4-7/Vermelho≥8, Reports, AuditHistoryModal), 8 riscos (array global sem paginação server-side, defect_name embutido em observations sem escaping, date como text lexicográfico, legado shift acentuado, PDF manual larguras fixas, fetchDefectData no-op, estados órfãos totalRecords/isSyncing/dbCompanyId, RLS depende de get_user_company_id) e 14 checklist. Ganchos futuros: get_revision_bootstrap, get_revision_defects_list paginada, save_defect_record atômica {ok,already,id,action}, delete_defect_record idempotente, get_revision_export_payload — todos SECURITY DEFINER + search_path=public + v_caller=get_user_company_id() + GRANT anon/authenticated/service_role. Nada implementado — apenas documentação.
  - **18/07/2026 (Brasília) — docs/rpcproduction.md recriado:** Plano de migração em 4 fases para src/pages/Production.tsx + src/lib/queries/productionsQueries.ts + escritas de produção em src/hooks/useCompanyData.ts, seguindo padrão consolidado (rpcInvoices/rpcclientInvoices/rpcstockMalha/rpcBillingOrders/rpcFreightOrders/get_faturamento_total_metrics). Regra imutável de reversion_point_production_logic.md preservada — eficiência, weight_kg e revenue seguem calculados 100% no cliente (preview do modal e calculateShiftDowntime intocados). Fase 1 get_production_bootstrap (company + shift_settings + last_production_date + available_months + stats, elimina useEffect que varre productions para descobrir última data e libera Production.tsx do array global); Fase 2 get_productions_by_shift paginada server-side com agrupamento machine_id|date|created_at direto no SQL via jsonb_agg (KPIs + groups + total_groups + page), avg_efficiency exclui grupos com total_rolls=0, ordenação por machines.number ASC + created_at ASC idêntica ao JS, busca ILIKE em machine/weaver/article; Fase 3 auxiliares (get_production_duplicate_check atômico e get_production_group_detail para openEditGroup direto da RPC); Fase 4 save_production_group(p_id?,p_payload,p_items,p_author_name,p_author_code) grava cabeçalho + N itens em transação única compartilhando mesmo created_at (chave do grupo), mesma efficiency combinada em todos os itens, valida date ±5 anos, shift IN(manha,tarde,noite), rpm>0, efficiency 0-100, jsonb_array_length>=1, detecta duplicidade via índice único ux_productions_shift(company_id,machine_id,date,shift,created_at) devolvendo {ok:false,conflict:duplicate_shift} sem exception, UPDATE faz DELETE+INSERT com mesmo created_at preservando chave; delete_production_group idempotente. Todas SECURITY DEFINER search_path=public com v_caller := get_user_company_id() + Acesso negado, GRANT anon/authenticated/service_role, contrato {ok,already,conflict} anti-double-click. Documento inclui refactor cliente por fase (useDeferredValue no searchQuery, invalidação seletiva de queries, saveQueue/advanceToNext preservados, useAuditLog mantido), 12 regras críticas e checklist em 4 fases. Nada implementado — apenas planejamento; getProductions() permanece no useCompanyData por retro-compat de Dashboard/Reports/Weavers.
  - **18/07/2026 (Brasília) — docs/rpcFreightOrders.md criado:** Plano de migração em 4 fases para src/pages/FreightOrders.tsx + src/hooks/useFreightOrders.ts + src/components/freight/FreightReportsTab.tsx + src/lib/freightOrderPdf.ts, seguindo o padrão consolidado (rpcBillingOrders/rpcInvoices/rpcclientInvoices/rpcstockMalha/get_faturamento_total_metrics). Fase 1 bootstrap (get_freight_orders_bootstrap → company, role_scope, freighter_id do caller, freighters, cost_companies, stats por status, available_months completed_at America/Sao_Paulo, next_ofr_number server-side); Fase 2 leituras paginadas server-side (get_freight_orders_list com p_view open/in_progress/completed/cancelled/all + busca 8 campos + freighter/cost_company/mês/período/paginação, itens aninhados via jsonb_agg, photos_count sem URL, escopo freteiro injetado server-side; get_freight_order_detail para modal); Fase 3 escritas atômicas — create_freight_order (SELECT FOR UPDATE sentinela para ofr_number sem race, elimina retry 5x + rollback manual, snapshot server-side cost_company_name/article_name/yarn_type_name), update_freight_order (só em open, delete+insert de itens em transação SQL única, devolve freighter_changed + old/new_freighter_id), start_freight_pickup / complete_freight_order (valida 1–2 fotos server-side, freight_total = SUM(items.weight_kg)*price sempre server-side, delivery_doc_* com COALESCE) / cancel_freight_order (motivo obrigatório), save/delete_freighter, save/delete_cost_company; Fase 4 auxiliares (get_freight_reports_metrics KPIs+listagem paginada 20/pg com escopo por role — freteiro nunca recebe revenue/profit; get_freight_orders_export payload PDF pronto; get_freight_order_pdf_payload elimina 5 joins de profiles). Todas SECURITY DEFINER com v_caller_company := get_user_company_id() + Acesso negado, GRANT anon/authenticated/service_role, idempotência {ok,already,conflict}, auditoria server-side, transição condicional WHERE status=expected, snapshots preservados no INSERT. Documento inclui refactor por fase, Realtime (canal freight_orders_rt_${companyId} só invalida chaves — preserva memory features/ofr-realtime), 13 riscos críticos e checklist de validação em 14 pontos. Nada implementado — apenas planejamento.
  - **18/07/2026 (Brasília) — docs/FreightOrders.md criado:** Documentação técnica completa de src/pages/FreightOrders.tsx (1.379 linhas) + src/hooks/useFreightOrders.ts (589 linhas) + src/components/freight/FreightReportsTab.tsx (684 linhas) + src/lib/freightOrderPdf.ts (278 linhas) — propósito, permissões (admin, lider_frete, freteiro com RLS por freighter.user_id), modelo de dados (freighters, freight_cost_companies, freight_orders 28 col com auditoria completa de transições e freight_total server-side, freight_order_items com snapshots article_name/yarn_type_name, freight_order_photos bucket privado freight-photos com createSignedUrl), hook (5 queries + realtime único canal freight_orders_rt_${company_id} invalidando 5 tabelas, nextOfrNumber com retry 5x anti-23505, createOrder com rollback manual se itens falharem, updateOrder só em open com delete+insert de itens, startPickup/completeOrder/cancelOrder condicionais por status, uploads no bucket privado com crypto.randomUUID, freight_total = round(kg*preço*100)/100 calculado server-side, push notifications include_admins + target_user_ids do freteiro com source=OFR/ref_id/ref_number para dedup), página (5 abas open/in_progress/completed/cancelled/reports, contador removido em reports, busca ampla em 8 campos, ticker 1s para cronômetro elapsed, paginação 15/pg só em Finalizados, modais NewOFR/Complete/Cancel/Details/FreightersManager/CostCompanies), FreightReportsTab (filtro padrão mês corrente, KPIs Rolos/Peso/Receita/Custo/Lucro com receita e custo ocultos para freteiro, paginação 20/pg janela deslizante, export PDF jsPDF+autoTable com logo), freightOrderPdf (cabeçalho colorido por status, timestamps de cada transição, duração total, fotos embutidas via dataURL, rodapé auditoria), 10 regras críticas (ofr_number único com retry, snapshot cost_company_name, rollback manual, edição só em open, UPDATEs condicionais, fotos obrigatórias 1-2, freight_total server-side, cancelamento com motivo, autoria Nome #Código, realtime obrigatório), riscos (nextOfrNumber varre tabela inteira, query com 5 joins profiles, agregação de relatórios 100% client-side, updateOrder não é transação SQL única) e checklist de 11 comportamentos esperados. Ganchos futuros: get_next_ofr_number, get_freight_orders_list paginada, get_freight_reports_metrics, save_freight_order atômica. Nada implementado — apenas documentação.
  - **18/07/2026 (Brasília) — docs/rpcBillingOrders.md criado:** Plano de migração em 4 fases para src/pages/BillingOrders.tsx (3.189 linhas) + src/hooks/useBillingOrders.ts (778 linhas), seguindo o padrão consolidado (rpcInvoices/rpcclientInvoices/rpcstockMalha/get_faturamento_total_metrics). Fase 1 bootstrap (get_billing_orders_bootstrap → company, stats por status incluindo awaiting_doc, available_months collected_at America/Sao_Paulo, next_of_number server-side, link_groups_count); Fase 2 leituras paginadas server-side (get_billing_orders_list com p_view priority/open/separating/awaiting_doc/ready/collected/cancelled/all + busca/cliente/mês/período/paginação, pallets aninhados via jsonb_agg, link_group_size via COUNT OVER PARTITION, ordenação priority DESC/created_at DESC ou collected_at DESC; get_billing_order_detail para modal); Fase 3 transições atômicas substituindo useBillingOrders inteiro — start_billing_order_separation (UPDATE condicional status=open), launch_billing_order_ready (sincroniza billing_order_pallets delete+insert, aplica delta em stock_movements com regra sem release fantasma via LEAST, out net por (machine_id,is_second_quality), own_stock isolado, recalcula weight_avg, tudo transacional), set_billing_order_delivery_doc (só em ready + delivery_doc_number IS NULL), collect_billing_order, revert_billing_order_to_open (SET NULL intencional em delivery_doc_*/pieces_real/weight_real/separated_by/collected_by, preserva reversal_quality, reaplica inverso netByKey + restoreOwnStockForOrder embutido), cancel_billing_order (respeita reversal_quality), edit_billing_order (COALESCE + campos vetados), set_billing_order_priority, create_billing_order (of_number server-side com SELECT FOR UPDATE sentinela, devolve duplicate_of_number amigável em vez de 23505), link/unlink/remove_from_group (merge escolhe menor UUID, dissolve grupo com 1); Fase 4 auxiliares (get_billing_orders_export payload PDF pronto, get_billing_order_negative_warning server-side, get_billing_order_link_group). Todas SECURITY DEFINER com v_caller_company := get_user_company_id() + Acesso negado, GRANT anon/authenticated/service_role, idempotência {ok,already,conflict}, auditoria server-side, transição condicional WHERE status=expected. Documento inclui refactor por fase, realtime (canal billing-orders só invalida chaves), 10 riscos críticos (sem release fantasma, out por máquina, machine_id 1ª qualidade, own_stock isolado, delivery_doc só ready, of_number único, reversal_quality preservada, link_group merge menor UUID, auditoria, idempotência) e checklist de validação em 11 pontos. Nada implementado — apenas planejamento.
  - **18/07/2026 (Brasília) — docs/BillingOrders.md criado:** Documentação técnica completa de src/pages/BillingOrders.tsx e src/hooks/useBillingOrders.ts — propósito, tabelas (billing_orders 42 col + billing_order_pallets + stock_movements + own_stock_movements), permissões (admin vs expedicao), máquina de estados (open→separating→ready→collected + cancel/estorno + revertToOpen) com todos os efeitos de estoque (reserve/release/out/in por máquina, saldo líquido anti release-fantasma, 1ª/2ª qualidade, own_stock preservado), hook (query com joins nomeados, realtime, createOrder anti-duplicidade of_number + 23505, updateStatus condicional expectedStatus, editOrder revertToOpen limpa doc/reais/separador, setDeliveryDoc só em ready, linkOrders/unlinkGroup/removeFromGroup, getNextOfNumber, restoreOwnStockForOrder), página (7 abas priority_tab/open/separating/awaiting_doc/ready/collected/cancelled, filtros, memos filteredOrders/sortedCollected/paginatedOrders/linkGroups/stats, handlers create/launch/priority/collect/cancel/edit/doc/print, modais paletes/detalhes/atrelar, negativeWarning proativo, conflictInfo), impressão jsPDF com cabeçalho colorido por status e seções auditoria/paletes, regras críticas (10 itens incluindo sem reserva duplicada quando há paletes, baixa out por máquina via netByKey, estorno preserva reversal_quality, own_stock isolado do stock_movements, sem release fantasma, updates condicionais, of_number único, doc só em ready, mesclagem de link_group_id) e checklist de 11 comportamentos esperados. Nada implementado — apenas documentação.
  - **18/07/2026 (Brasília) — docs/rpcstockMalha.md criado:** Plano de migração em 4 fases para src/pages/StockMalha.tsx seguindo o padrão consolidado (get_faturamento_total_metrics / docs/rpcInvoices.md / docs/rpcclientInvoices.md). Fase 1 bootstrap único (get_stock_malha_bootstrap → company + cutoff_date + available_months + own_articles, elimina os dois loops while(hasMore) de invoices/invoice_items usados só para descobrir meses); Fase 2 leituras server-side por aba (get_stock_malha_estoque com CTEs cutoff+prod+mov+mov_agg replicando 1:1 malhaEstoque/byMachineMap incluindo by_machine embutido, KPIs e regra "NF de saída não desconta"; get_stock_malha_segunda isolado de cutoff/produções; get_own_stock_summary com lotes por yarn_type+of_number+source+outsource_company_id; get_stock_malha_movements paginado server-side, elimina LIMIT 500); Fase 3 auxiliares (get_stock_malha_article_export para PDF por artigo com apenas máquinas ≥1 rolo, e get_own_stock_movements_history opcional); Fase 4 escritas atômicas (save_stock_manual_movement, save_own_stock_article, save_own_stock_movement, delete_own_stock_movement) com SECURITY DEFINER + guarda p_company_id = get_user_company_id() (herança do pente fino de rpcclientInvoices), SELECT FOR UPDATE, idempotência {ok,already} e RAISE EXCEPTION amigável. Documento inclui algoritmo SQL detalhado, tabela Q1–Q8 do JS atual, plano de refactor no cliente aba a aba, Realtime (canal stock-malha-realtime-${companyId} passa a invalidar chaves das RPCs), riscos (cutoff obrigatório em ambas as tabelas, machine_id obrigatório na 1ª qualidade, in c/OF é estorno vs in s/OF é entrada manual, NFs de saída jamais entram no cálculo, aba Movimentações mostra tudo inclusive < cutoff) e checklist de validação em 11 pontos. Nada implementado — apenas planejamento.
  - **18/07/2026 (Brasília) — docs/stockMalha.md criado:** Documentação técnica completa de src/pages/StockMalha.tsx — propósito, fórmulas (Produzido/EntregueTotal/Reservado/Físico/Disponível, cutoff, regra "NF de saída não desconta"), tabelas envolvidas (stock_movements, productions, own_stock_*, company_settings.stock_cutoff_date + RLS), fetch (fetchAllPaginated + 8 chaves React Query + isStockLoading agregado), realtime (canal stock-malha-realtime-${companyId} + evento stock-movements-changed), estado local, memos (malhaEstoque, byMachineMap, segundaEstoque, ownSummary/ownDetailByArticle, KPIs, paginação Movimentações), UI das 4 abas (Estoque/Própria/2ª Qualidade/Movimentações), export PDF por artigo com quebra por máquina, modais ManualStockEntryModal/OwnStockManualModal, permissões (stock_malha em enabled_nav_items), riscos (peso client-side → candidato a RPC, cutoff, machine_id obrigatório) e ganchos futuros (get_stock_malha_bootstrap/get_stock_malha_movements/save_manual_stock_movement). Nada implementado — apenas documentação.
  - **18/07/2026 (Brasília) — Hotfix get_client_invoices_by_client:** Rede reportou 400 "CREATE TABLE AS is not allowed in a non-volatile function" ao abrir aba individual de cliente em Notas Fiscais (Clientes). Causa: função marcada STABLE usava CREATE TEMP TABLE _base/_filt (proibido em STABLE/IMMUTABLE). Reescrita 100% em CTEs (links_agg → legacy_agg → items → base → filt → totals → paged → rows_json) mantendo mesma lógica de saldo/KPI/paginação/anti-dupla-contagem legacy vs link; aberto continua sem paginação, encerrada/historico paginam. Contrato JSON idêntico ({rows,total_count,kpis}), guarda multi-tenant preservada. Cliente inalterado.
  - **18/07/2026 (Brasília) — Pente fino Fases 1–3 rpcclientInvoices.md:** Auditoria detectou brecha crítica de isolamento multi-tenant nas 7 RPCs de leitura SECURITY DEFINER (get_client_invoices_bootstrap, get_client_invoices_search, get_client_invoices_by_client, get_client_invoice_linked_exits, get_client_invoice_balances_for_distribute, get_client_invoices_export, get_client_invoice_by_nf_export) — nenhuma comparava p_company_id com public.get_user_company_id(), permitindo que usuário autenticado da empresa A lesse dados de qualquer empresa B conhecendo o UUID. Corrigido em migration única: cada função valida v_caller := get_user_company_id() e aborta cedo devolvendo payload vazio (rows/entries []; kpis {}; totalEntrada/Saida/Saldo 0; entry NULL); get_client_invoice_by_nf_export devolve {ok:false,error:forbidden}. Fase 4 (save_client_invoice/delete_client_invoice) já validava tenant. Contratos JSON e cliente preservados — nenhuma alteração funcional para usuários legítimos.
  - **18/07/2026 (Brasília) — Fase 4 rpcclientInvoices.md implementada:** Criadas 2 RPCs SECURITY DEFINER search_path=public GRANT anon/authenticated/service_role em migration única — (1) save_client_invoice(p_id?,p_payload,p_items,p_exit_links,p_author_name,p_author_code) grava cabeçalho + item único (delete+insert) + exit_links (delete+insert quando saída) em transação SQL única; valida get_user_company_id() vs company_id do payload (INSERT) e da linha via SELECT FOR UPDATE (UPDATE), type IN(entrada,saida), issue_date ±5 anos, weight_kg>0, entrada⇒yarn_type_id obrigatório, saída⇒article_id + ≥1 link válido + sem duplicidade entry_invoice_id + SUM(deduct_kg)≤weight+0.001 + cada entry deve ser type=entrada do mesmo client_id/company_id (NOT EXISTS); composição sempre NULL; devolve {ok,already,id,action}; already:true quando UPDATE aponta para linha inexistente (anti double-click). (2) delete_client_invoice(p_id,p_author_name,p_author_code) faz SELECT FOR UPDATE, valida tenant, conta links (DISTINCT exit_invoice_id em exit_links) + legacy (parent_invoice_id sem overlap com links via NOT EXISTS), executa DELETE (CASCADE cuida do resto), devolve {ok,already,cascade_count,was_parent}. Cliente src/pages/ClientInvoices.tsx: saveInvoiceMutation reduzido de ~150 linhas para chamada única supabase.rpc('save_client_invoice',...) — removidas todas as validações locais (agora server-side), snapshot manual de links e rollback manual best-effort; onSuccess só loga/toast quando !already e invalida ['client_invoices','client_invoice_exit_links','client_invoices_search','client_invoices_by_client','client_invoice_linked_exits','client_invoices_bootstrap']. confirmDelete idem: chama delete_client_invoice, usa cascade_count no toast ("Nota excluída — N saída(s) vinculada(s) também foram removidas"), respeita already, invalida mesmas chaves. Nenhum INSERT/UPDATE/DELETE direto sobra em client_invoices/client_invoice_items/client_invoice_exit_links nesta página. tsgo --noEmit limpo. Migração rpcclientInvoices.md concluída (Fases 1→4).
  - **18/07/2026 (Brasília) — Fase 3 rpcclientInvoices.md implementada:** Criadas 3 RPCs SECURITY DEFINER STABLE search_path=public GRANT anon/authenticated/service_role — (1) get_client_invoice_balances_for_distribute(company,client,entry_ids[],exclude_exit_id?) devolve {entries:[{entry_invoice_id,weight_entrada,already_consumed,saldo}]} com CTEs ent+links_agg+legacy_agg exatamente iguais à Fase 2 mas com filtro p_exclude_exit_id para ignorar o próprio exit_invoice_id ao editar (mesma regra do JS antigo); (2) get_client_invoices_export(company,client,type,month,start,end) devolve {company,client,rows[{issue_date,invoice_number,type,yarn_name,supplier_name,weight_entrada,weight_saida,saldo,article_name}],totals{totalEntrada,totalSaida,totalSaldo,totalNotas}} — yarn_name na saída resolvido via string_agg dos yarn_type_id dos exit_links do próprio exit (fallback vazio quando só existe legacy), saldos server-side eliminam recomputação; (3) get_client_invoice_by_nf_export(company,entry_id) devolve {company,client,entry(com weight_entrada+yarn_type_name resolvidos),linked[{...,article_name,source:link}],legacy[{...,source:legacy}],consumed_kg,saldo} com NOT EXISTS anti-dupla-contagem, retorna {ok:false,error:not_found} quando entry não é do tenant/não é entrada. src/lib/clientInvoicePdf.ts refatorado: exportClientInvoicesGeneralPdf agora recebe {rows,totals,clientName,exportType} sem passar exitLinksAll/allClients/allArticles/yarnTypes; exportClientInvoiceByNfPdf recebe {entry,client,linked,legacy,consumed_kg,saldo} sem exits/allArticles/yarnTypes — nenhum cálculo de saldo ou lookup no PDF, só desenha. src/pages/ClientInvoices.tsx: botão Wand2 Auto distribuir vira async e consome get_client_invoice_balances_for_distribute (saldo fresco, sem depender de exitLinksAll); handleExport (general) e handleExportSingleNf (by_nf) consomem as RPCs de export e passam payload direto para os geradores. Legacy useQuery(client_invoices) e useQuery(client_invoice_exit_links) permanecem vivos porque ainda alimentam handleEditInvoice (setExitLinks), monthOptions do modal de export (fallback), onAddFromClient (parent yarn), tabela de seleção do "Por NF" e modal Saídas Vinculadas — Fase 4 (escritas atômicas) removerá o restante. tsgo --noEmit limpo.
  - **18/07/2026 (Brasília) — Fase 2 rpcclientInvoices.md implementada:** Criadas 3 RPCs SECURITY DEFINER STABLE search_path=public GRANT anon/authenticated/service_role — (1) get_client_invoices_search(company,search,month,type,page,page_size) devolve {rows:[client_invoices + client_name + items[] com yarn_type_name/article_name resolvidos via JOIN], total_count} paginada server-side, ILIKE em invoice_number/clients.name/supplier_name/items.yarn_type_name/items.article_name; (2) get_client_invoices_by_client(company,client,view,search,page,page_size) devolve {rows,total_count,kpis:{totalEntrada,totalSaida,totalSaldo}} — CTEs entradas+items+links_agg+legacy_agg calculam weight_entrada/weight_saida/saldo/is_encerrada/has_linked_outputs server-side (algoritmo replica invoicesWithBalance do JS 1:1, NOT (linked_exit_ids @> ARRAY[s.id]) evita contagem dupla legacy vs link), view aberto devolve tudo, encerrada/historico paginam, KPIs sobre filtro completo pré-paginação; (3) get_client_invoice_linked_exits(company,entry_id) devolve {entry,linked[source=link],legacy[source=legacy],consumed_kg} para o modal. Cliente src/pages/ClientInvoices.tsx: Busca Geral em useQuery([client_invoices_search],30s) com debounce 300ms e paginação server-side (removido filtro+slice locais; itens usam yarn_type_name/article_name do payload com fallback ao CompanyDataContext). ClientDetailView em useQuery([client_invoices_by_client],30s) com debounce 300ms, stats vira serverKpis, filteredInvoices só normaliza snake→camelCase (weight_entrada→weightEntrada, is_encerrada→isEncerrada, has_linked_outputs→hasLinkedOutputs), paginação client-side removida. Legacy useQuery(client_invoices) e useQuery(client_invoice_exit_links) mantidos vivos porque ainda alimentam handleEditInvoice, auto-distribuir, modal de linked exits, handleExportSingleNf e monthOptions — Fases 3/4 removerão. tsgo --noEmit limpo.
  - **18/07/2026 (Brasília) — Fase 1 rpcclientInvoices.md implementada:** Criada RPC get_client_invoices_bootstrap(p_company_id) SECURITY DEFINER STABLE search_path=public GRANT anon/authenticated/service_role, devolvendo {company:{name,logo_url}, available_months:[YYYY-MM DESC]} — meses via DISTINCT to_char(issue_date,YYYY-MM) UNION mês corrente America/Sao_Paulo. Cliente src/pages/ClientInvoices.tsx: substituído useEffect(companies) + companyLogoUrl/companyName useState por useQuery([client_invoices_bootstrap], staleTime 5min); dropdown Filtrar Mês da Busca Geral agora consome bootstrap.available_months (com fallback para o cálculo antigo baseado em clientInvoices caso a lista venha vazia). Nenhuma alteração em Q1/Q2 (client_invoices/exit_links) nem em mutations — reservados para Fases 2–4. tsgo --noEmit limpo.
  - **18/07/2026 (Brasília) — docs/rpcclientInvoices.md criado:** Plano de migração em 4 fases para src/pages/ClientInvoices.tsx seguindo o padrão consolidado (get_faturamento_total_metrics / docs/rpcInvoices.md). Fase 1 bootstrap (company + available_months + clients_with_activity); Fase 2 leituras paginadas server-side (get_client_invoices_search, get_client_invoices_by_client com saldo/KPIs já calculados via CTEs entradas+links_agg+legacy_agg — elimina invoicesWithBalance do JS e a contagem dupla legacy vs link, get_client_invoice_linked_exits para o modal); Fase 3 auxiliares e export (get_client_invoice_balances_for_distribute para auto-distribuir, get_client_invoices_export e get_client_invoice_by_nf_export para PDFs); Fase 4 escritas atômicas em transação SQL única (save_client_invoice, delete_client_invoice com cascade_count, sync_client_invoice_exit_links opcional) com validação obrigatória de get_user_company_id() (herança do pente fino), idempotência {ok:true,already:true} e COALESCE em UPDATEs. Documento inclui algoritmo SQL do saldo replicando §7 de docs/clientInvoices.md, plano de refactor no cliente, realtime, riscos (contagem dupla legacy, composition legado, parent_invoice_id legado, 1 item por NF invariante) e checklist de validação em 11 pontos. Nada implementado — apenas planejamento.
  - **18/07/2026 (Brasília) — Pente fino Fases 1–4 rpcInvoices.md:** Auditoria completa das RPCs. Detectada brecha crítica de isolamento multi-tenant: cancel_invoice, confirm_invoice, delete_yarn_type, delete_outsource_yarn_stock, save_outsource_yarn_stock (UPDATE) e save_invoice (INSERT+UPDATE) eram SECURITY DEFINER mas não validavam se o registro alvo pertencia à empresa ativa — permitindo que usuário da empresa A, conhecendo o UUID, deletasse/cancelasse/editasse dados da empresa B. Corrigido em migration única: todas chamam public.get_user_company_id() no início, comparam com company_id do payload (INSERT) e/ou da linha via SELECT FOR UPDATE (UPDATE/DELETE), abortando com RAISE EXCEPTION 'Acesso negado'. save_invoice UPDATE ganhou COALESCE para preservar access_key/observations/client_id/client_name/buyer_name/destination_name (antes sobrescrevia com NULL quando ausentes) e validação server-side ±5 anos em issue_date. RPCs get_* das Fases 1–3 auditadas sem gaps (já filtram por p_company_id + RLS nas tabelas base). UNIQUE (company_id, outsource_company_id, yarn_type_id, reference_month) confirmada em outsource_yarn_stock. Cliente src/pages/Invoices.tsx inalterado — contrato preservado.
  - **18/07/2026 (Brasília) — Notas Fiscais (Trama) — Fase 4 rpcInvoices.md (escritas atômicas):** Criadas 7 RPCs SECURITY DEFINER search_path=public GRANT anon/authenticated/service_role em migration única — (1) save_invoice(p_id?,p_payload,p_items,author) grava NF+itens em transação única (INSERT quando p_id=null; UPDATE+DELETE+INSERT itens quando editando), recomputa total_weight_kg/total_value/subtotal server-side, valida type/data/access_key (44 dígitos); (2) confirm_invoice(p_id) → SELECT FOR UPDATE + UPDATE status=conferida, devolve already:true se já conferida ou removida; (3) cancel_invoice(p_id) aplica dentro da RPC a regra type=entrada⇒DELETE físico / demais⇒status=cancelada — mesma que estava no cliente; (4) save_yarn_type / (5) delete_yarn_type / (6) save_outsource_yarn_stock (preserva ON CONFLICT company/outsource/yarn/month DO UPDATE quando p_id=null; UPDATE parcial de quantity/observations quando editando) / (7) delete_outsource_yarn_stock — todas devolvem {ok:true} ou {ok:true,already:true} anti double-click com SELECT FOR UPDATE. Cliente src/pages/Invoices.tsx: handleSaveInvoice/handleCancelInvoice/handleConfirmInvoice/handleSaveYarn/handleDeleteYarn/handleSaveEft/handleDeleteEft agora chamam RPCs via supabase.rpc; nenhum INSERT/UPDATE/DELETE direto sobra em invoices/invoice_items/yarn_types/outsource_yarn_stock nesta página. Trato de idempotência: logAction/toast só disparam quando data?.already é falso (evita duplicar auditoria em double-click). Invalidações continuam via invalidateInvoiceCaches() (Fase 3). Total pages/cache/UX inalterados. tsgo --noEmit limpo. Migração rpcInvoices.md concluída (Fases 1→4).
